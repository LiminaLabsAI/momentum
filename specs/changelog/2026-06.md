# Changelog — 2026-06

## 2026-06-07

- Phase 9 brainstormed — Ecosystem (Tier 1). Multi-repo coordination
  layer for momentum. Adds an ecosystem manifest, cross-repo
  initiative concept, and auto-logged daily session file threading
  per-repo events. Single-repo momentum unchanged when no ecosystem
  root is present. Decisions locked: separate git repo for ecosystem
  state; auto-log via PostToolUse hook; `momentum ecosystem add` for
  opt-in. Targets v0.12.0. PR #1 merged to main + staging.
- Phase 9 bookkeeping landed: `specs/status.md` active phase →
  Phase 9; `specs/planning/roadmap.md` renumber Hardening→10, Reach→11,
  Intelligence→12, Platform→13 with version targets shifted by one
  release; `specs/phases/index.json` adds `phase-9-ecosystem-tier1`
  entry. PR #2 merged to main + staging.
- Phase 9 started on branch `phase-9-ecosystem-tier1-impl`. Group 0
  (schemas + layout) up next.
- Phase 9 Group 0 landed: `core/ecosystem/schema/ecosystem.schema.json`
  + `initiative.schema.json`, `core/ecosystem/layout.md`,
  zero-dependency `core/ecosystem/lib/index.js`
  (findRoot/loadManifest/listMembers/findMember/validateManifest).
- Phase 9 Groups 1-3 landed: `bin/ecosystem.js` exposes `momentum
  ecosystem init/add/remove/status` (`init` scaffolds + git-inits the
  ecosystem root; `add` is idempotent + writes fenced pointer; `remove`
  inverses); `core/ecosystem/lib/initiative.js` + template + `/initiative`
  recipe; `core/ecosystem/scripts/session-append.sh` + extended
  PostToolUse hook auto-logs `git commit` and `gh pr` events into the
  daily session file. `momentum init` + `upgrade` ship the session
  helper alongside the hook.
- Phase 9 Groups 4-6 landed: `/ecosystem` + `/session` slash command
  recipes; 30+ new tests (`tests/ecosystem-cli.test.js`,
  `tests/ecosystem-initiative.test.js`, `tests/ecosystem-hook.test.js`,
  101/101 green up from 64); `core/specs-templates/specs/architecture/
  ecosystem.md` shipped to every momentum project; README "Ecosystems"
  section + worked cerebrio example.
- Phase 9 `/sync-docs` run: registered ten new ecosystem topics in
  `specs/decisions/impact-map.json` for future-phase routing. No other
  spec edits needed — implementation updated everything inline. PR #4
  merged to main + staging.
- **Phase 9 Complete → v0.12.0 Released.** Retrospective populated with
  Rule 12 verification evidence (fresh `npm test` capture + end-to-end
  smoke commands). Active phase → Phase 10 (Hardening & Activation,
  target v0.13.0). cerebrio constellation queued as Phase 9 dogfooding
  ground (initiative `0001-memory-module` will retroactively capture the
  2026-06-05/06 Memory module v1 session once the ecosystem repo is
  bootstrapped).
- Deferred Context Economy phase brief landed at
  `specs/planning/future-context-economy.md` (5-agent workflow:
  audit + 3 parallel angles + synthesis). Two survivors after the
  quality bar: Section Map headers (additive, Rule-1-adjacent
  guidance — explicitly NOT a new Rule 13) and ecosystem `STATE.md`
  router (router-only, banner + path-suffix invariants prevent
  quotation). Lifecycle & Archival rejected entirely as premature.
  Locked evaluator first per Rule 11. Triggers are size/behaviour
  based, not time-based. Tracked as ENH-020 (P3, deferred).
- **Phase 10 brainstormed → Ecosystem Activation & Polish** (target
  v0.13.0). Five workstreams: top-level entry/exit commands
  (`init --ecosystem`, `join`, `leave`, `doctor`, init auto-detect);
  Phase 9 follow-up fixes (BUG-004 session-log race, ENH-021
  location-agnostic `ecosystem add`, ENH-022 bounded-walk constant);
  product positioning README rewrite (decision tree, two-scale model,
  supported-agents matrix, quickstarts); per-adapter smoke matrix +
  Adapter Contract v3 capability flag audit (Claude Code, Codex,
  Antigravity); cerebrio dogfood (`cerebrio-ecosystem/` bootstrap +
  `0001-memory-module` initiative). Hard invariant: single-project
  usage identical to today. Phase files at
  `specs/phases/phase-10-ecosystem-activation/`.
- **Roadmap restructured.** Originally-planned Phase 10 (Hardening
  & Activation) moved to Unscheduled Future Work
  (`specs/planning/unscheduled-hardening-activation.md`). New
  Phase 11 (Dynamic Orchestration & Context Handover, target
  v0.14.0) captures the user-reframed orchestration story:
  capability-driven `scout` / `dispatch` / `handoff` primitives the
  main agent composes per task, with specs maintenance preserved
  across every primitive. Planning stub at
  `specs/planning/phase-11-orchestration-handover.md`. Reach /
  Intelligence / Platform each shift one slot; v1.0 target unchanged.
- BUG-004 / ENH-021 / ENH-022 filed to backlog as Phase 9 follow-ups
  discovered during Phase 10 review. All three fold into Phase 10
  Workstream 2.
- **Phase 10 started.** Status flipped to `In Progress`. Execution
  begins with Group 0 (state machine + reusable pointer helpers,
  test fixtures) — sequential, blocks Groups 1/2/3. Once Group 0
  lands, the three implementation tracks run in parallel: entry/exit
  commands (G1), Phase 9 fixes (G2), README rewrite + onboarding
  clarity (G3). Group 4 (adapter coverage + cerebrio dogfood) and
  Group 5 (verification) close the phase.
