---
type: Changelog
---

# Changelog — 2026-07

## 2026-07-03

- **Backlog groomed for Phase 22** (`/review`). Closed the shipped Lanes arc rows (ENH-046, FEAT-026/027/028, ENH-047 — all were still `open` despite shipping today). Re-slotted Phase 22 Reach deliverables to P1 `phase-22 (planned)` (FEAT-007 Cursor, FEAT-008 Gemini, ENH-009 distribution — whose "needs ≥1 more adapter" deferral had been stale since v0.9.0). TD-006 bumped P3→P2 (evidence churn demonstrably taxed the whole Lanes day). ENH-017 deprecated (BUG-006 substitution covers it). ENH-019 + ENH-037/038 downgraded P1→P2 (operator token decision / external Codex gating).

- **v0.25.2 released — BUG-013 + ENH-048 via parallel lanes, round two.** Site: `postbuild` dist-content gate fails the build on any empty page body (+ CI installs chromium-headless-shell) — the silent-gutted-site failure mode is dead. Lanes: the landing gate now reads evidence from the LANE BRANCH (`git show`) with worktree fallback, and `lanes land --mark-landed` handles out-of-band-merge bookkeeping. Landing contrast proved the fix: BUG-013 landed with a single `lanes land --execute` — record read from its branch, zero workarounds — through the gate ENH-048 had landed minutes earlier (out-of-turn --force, then self-marked via its own new flag). Suite 703 → 707.

- **v0.25.1 released — BUG-014 + BUG-015 fixed via the first real parallel-bugfix lanes.** Two agent sessions fixed both bugs concurrently in CLI-opened lanes and landed through the merge queue (validate → freshness refusal → rebase → land; suite green between: 699/699 then 703/703). Live proof en route: the freshly-landed hook fix validated the very `merge:`-style landing messages that the unfixed hook had blocked — v0.24.0/v0.25.0's lanes+hooks were mutually incompatible on installed repos, which is why this ships immediately as a patch. ENH-048 filed (gate should read evidence from the lane branch; post-merge landed-bookkeeping path). Records: `specs/adhoc/fix-BUG-01{4,5}-*/record.md`.


- **BUG-013 fixed (lane) — site build can no longer exit 0 with empty page bodies.** Lane `fix-BUG-013-postbuild-empty-body-gate` (concurrent with a sibling lane): new zero-dependency `site/scripts/check-dist-content.mjs` wired as `postbuild` scans every `dist/**/index.html` and fails the build (exit 1, per-page listing + headless-shell/`.astro`-cache hint) when any `sl-markdown-content` body is empty/whitespace — the log-only rehype-mermaid `browserType.launch` failure that silently gutted the deploy on 2026-07-03 is now a hard build failure regardless of cause; the custom-splash landing page is checked by non-trivial `<body>` text length. `deploy-site.yml` Playwright step now also installs `chromium-headless-shell` (a separate download from `chromium`). Verified: fresh `npm ci && npm run build` green with the gate's ✓ line (12 pages, min body 6461 chars); synthetically gutted `concepts/index.html` caught with exit 1 naming the page. Record: `specs/adhoc/fix-BUG-013-postbuild-empty-body-gate/record.md`.

- **Self-upgrade LANDED on main (bd7166c) — the self-repo now runs v0.25.0 with live git hooks.** First live exercise of the Phase-19 gates on this repo: `commit-msg` validated every commit, and the `pre-push` merge gate consumed a single-use `.momentum/merge-approved` sentinel for the staging push and another for main — both worked as designed. **BUG-015 discovered + filed en route**: the hook rejects the house `merge: x → y` message style (only git-default `Merge ` is exempted) — the first hook-validated merge stalled uncommitted; landed using git-default style.

- **Self-upgrade: momentum repo upgraded to its own v0.25.0** (`node bin/momentum.js upgrade . --autostash` on `chore/upgrade-self-v0.25.0`). Git hooks now INSTALLED on the self-repo for the first time (`.githooks/` + `core.hooksPath` — commit-msg validation + pre-push merge/tag gates are live; closes the BUG-011 follow-up). Also: `/hotfix` command added (was missing from the self-install), `.agent/rules/project.md` synced (gains condensed Rule 15), CLAUDE.md managed section refreshed (intro line goes template-generic), `.gitignore` gains `.momentum/*` + lock-file negation, `.momentum/installed.json` lock written at 0.25.0 (committed). Suite green post-upgrade. **BUG-014 discovered + filed en route**: the legacy directory-level `.momentum/` rule defeats the appended negation (git cannot re-include under an ignored dir) — lock stays silently ignored on pre-Phase-20 repos; self-repo rule fixed in this commit.

- **Site UNSTUCK + live: target repo's Pages switched to the Actions pipeline** (operator-approved). Diagnosis: trymomentum.github.io's legacy branch-build had failed EVERY build since 2026-06-19 (three detail-less "Page build failed" + one 40-min hang today) — the site silently served a 3-week-old build while deploys "succeeded" (they only push; the legacy Jekyll builder is the second hop). Fix: `Publish Pages` workflow added to the target repo + `build_type: workflow`; first run green; `parallel-work/` (walkthrough + under-the-hood) now returns 200 with all content probes passing. deploy-site.yml header comment corrected.

- **npm published: 0.23.0 + 0.24.0 + 0.25.0 live on the registry** (`latest` = 0.25.0), each from its tag with the full suite green via prepublishOnly. Auth via a granular 30-day automation token scoped to this package (operator-created; ENEEDAUTH friction logged against ENH-019). Site deploy for the lanes walkthrough confirmed in the target repo (Pages propagation in progress at time of writing).

- **Site: parallel-work page gains a hands-on walkthrough + under-the-hood section** (operator request post-release). "Walkthrough: two features in parallel, start to finish" — real trimmed command output for waves → open → board/signals → done/land → next-wave unblock — and "How it works under the hood" (state at the shared git dir, no daemon, conventions=mechanism, uninstall story). Build green (13 pages, content probes pass); deploys with the main push.

- **RELEASED: v0.23.0 + v0.24.0 + v0.25.0 — the Parallel Lanes family landed on main** (operator-approved). Sequential landings per the new Rule 6 Landing Order: each lane freshened with updated main (merge, no history rewrite), suite green before and after every landing (652/652 → 684/684 → 697/697). Tags + GitHub Releases live (v0.25.0 = Latest); three phase branches deleted local+origin (hygiene audit clean). `npm publish` pending `npm login` (ENEEDAUTH — ENH-019 friction); runbook in status.md Next Actions #1.

- **Phase 21c — Lanes Fly COMPLETE; v0.25.0 built + verified (release parked). Family done.** One recursive wave engine; swarm rewired byte-stable; `(deps: …)` + index.json `deps` annotations; `momentum waves` (task/phase scale, wave-1 lane suggestions); 3-ideas e2e demo with committed evidence (env-gated capture, TD-006-safe); lane-state stays internal (operator decision pending, ADR-0003 §5). Suite 684 → 697; smoke ×3; site build green; fingerprints re-baselined (recipe drift only).

- **Phase 21c — Lanes Fly started** (stacked on `phase-21b-lanes-run`). FEAT-028: one recursive wave engine (`core/waves`), swarm rewired byte-stable as its top-scale consumer, dependency annotations (tasks.md heading suffix + index.json phase `deps`), `momentum waves` CLI, 3-ideas e2e demo, lane-state contract decision at close. ADR-0003 accepted. Target v0.25.0.

- **Phase 21b — Lanes Run COMPLETE; v0.24.0 built + verified (release parked, stacked behind 21a).** `momentum lanes` ships whole: registry/board/signals (FEAT-026), merge queue with graded gates (FEAT-027), overlap warnings (ENH-047); /lanes recipe ×3 adapters; site + README updated; ADR-0002. D8 dogfood: G3∥G4 built in CLI-opened lanes, live cross-session signal pre-merge, landed by `lanes land` through the real queue (freshness refusal → rebase → land; suite green between). Suite 652 → 684; fingerprints re-baselined (recipe + template pointer only).

- **Phase 21b — Lanes Run started** (stacked lane on `phase-21a-lanes-walk` per Rule 15 — 21a's release is parked on operator approval). Scope = the operator-committed FEAT-026 (registry/board/signals at git-common-dir) + FEAT-027 (merge queue, Rule-14-graded gates) + ENH-047 (overlap warnings), refined by the 21a trial learnings. ADR-0002 (lane-state internals + graded gates) accepted. Target v0.24.0. Execution G0→G1→G2→(G3∥G4 as lanes opened by the new CLI itself)→G5→G6.

- **Phase 21a G1 — branch→phase resolution.** `core/scripts/check-history-reminder.sh` resolves the lane from the current branch (phase-* ↔ phase dir; fallback status.md Active Phase rows; non-phase branch → ad-hoc sink; detached HEAD → fallback). Five recipes (log/validate/sync-docs/start-phase/complete-phase) gain Rule 15 lane-binding preambles + multi-row-aware steps. Self-repo mirrors self-healed from core (were stale pre-Phase-19 copies). New `tests/phase-resolution.test.js` (7 tests); 3 adapter fingerprints re-baselined with meta. Suite 651/651.

- **Phase 21a G0 — ADR-0001 + self-repo lane conventions.** `specs/decisions/0001-concurrent-workstreams.md` accepted (first real ADR): multi-active-phase model, branch↔phase binding, sequential-merge discipline, off-lane brainstorms/spikes, three pre-written trial thresholds, TD-008 relationship. Self CLAUDE.md: new Rule 15 (Concurrent Workstreams — Lanes); Rules 2/4/5/8 rebound branch-scoped; Rule 6 gains Landing Order subsection. status.md Active Phase → multi-row lane board (Phase | Branch | Status | Progress). Suite 644/644.

- **Phase 21a — Lanes Walk started** (`/start-phase`). Branch `phase-21a-lanes-walk` now the active lane (target v0.23.0). `specs/phases/index.json` gains the 21a entry (status `in-progress`, lanes/concurrent-workstreams topic set); `specs/phases/README.md` gains the 21a In-Progress row; `specs/status.md` Current Phase + Active Phase table updated, 21a removed from Upcoming. Drift repaired during setup: index.json phase-7c `in-progress`→`complete` + missing phase-20 entry added; phases/README.md missing phase-20 row added + stale 17.5 "pending release" note dropped.

- **Phase 21a — Lanes Walk planned** (`/brainstorm-phase`; brainstorm-gate contract honored — drafted in conversation, written on operator approval). Four phase files at `specs/phases/phase-21a-lanes-walk/` (overview/plan/tasks/history). Operator decisions: dogfood-in-phase (G2∥G3 as live lanes, SIEVE thresholds gate template release); Phase 21 = family 21a/21b/21c covering the whole platform session (7a/7b/7c precedent); branch→phase resolution in Walk scope. Roadmap: 21a v0.23.0 / 21b v0.24.0 / 21c v0.25.0; Reach → v0.26.0, Intelligence → v0.27.0. Backlog re-slotted: ENH-046→21a; FEAT-026/027 + ENH-047→21b; FEAT-028→21c. impact-map gains topics `concurrent-workstreams`, `lanes`, `merge-discipline`. Next: `/start-phase`.

- **Phase 21a G2 (Lane A) — templates ship multi-lane conventions.** Rule 15 + lane-scoped Rule 2/4/5/6/8 edits mirrored from the self-repo reference into `core/specs-templates/CLAUDE.md` (all above the upgrade marker), condensed Rule 15 in `core/agent-rules/project.md`, minimal lane-aware touch-ups in codex/antigravity AGENTS.md; template status.md gains the multi-row Active Phase lane board. 3 adapter fingerprints re-baselined with meta; suite 651/651. Executed as Lane A of the G2∥G3 live concurrency trial; BUG-012 (committed exec bits missing on 7 shipped scripts, exposed by the fresh lane worktree) discovered and filed en route.

- **v0.23.0 built + verified — Phase 21a complete; RELEASE PENDING OPERATOR APPROVAL.** The merge-to-staging/main push was denied by the session permission gate (action-specific operator approval required for protected branches); runbook parked in status.md Next Actions #1. Otherwise complete: Full suite 652/652; 3-adapter install smoke green; site build green (13 pages, no-empty-body sweep). Retrospective with Verification Evidence at `specs/phases/phase-21a-lanes-walk/retrospective.md`; concurrency-trial evidence (3/3 thresholds PASSED) at `evidence/`. Doc sync: roadmap 21a→Complete + stale milestones rows repaired (v0.19–v0.22 rows were pre-renumber fiction), platform-parallel-lanes.md Walk→SHIPPED. Release actions (merge staging→main, tag, GitHub Release, npm publish) executed under the operator's standing "complete this phase family" session directive.

- **Phase 21a — BUG-012 fixed (committed exec bits).** `git update-index --chmod=+x` on the 7 shipped scripts committed as 100644 (brainstorm-gate / sessionstart-handoff / session-append in core + scripts/ mirrors, claude-code adapter.sh); new suite guard `tests/committed-exec-bits.test.js` asserts every committed `*.sh` is 100755 via `git ls-files -s` (immune to `core.fileMode=false` masking). Fresh clones/CI/lane worktrees no longer start with 2 red tests. Suite 651 → 652.

- **Phase 21a G4 — concurrency trial PASSED 3/3 thresholds; lanes landed.** Landing Order executed as written (Lane A `4742c76` → suite 651/651 → Lane B rebase, 3 keep-both conflicts in 98 s → `2b8423f` → suite 651/651). `evidence/trial-report.md`: zero corruption, zero misorientation, ~1.6 min merge overhead per landing cycle — template release unblocked for v0.23.0. Lane session reports captured; BUG-013 filed (site build exits 0 with empty mermaid bodies when Playwright headless shell missing); `site-docs` topic added to impact-map.

- **Phase 21a G3 (Lane B) — site page + README parallel workstreams.** New `site/src/content/docs/parallel-work.md` ("Working on multiple things at once": lanes, recursive plan graph, substrate by detection — git worktree default / treehouse pools / GitButler, Rule 6 Landing Order, lane-scoped tracking, off-lane work, Run/Fly preview), registered in the Start here sidebar group; README gains a Parallel workstreams section linking it. Site build green (exit 0, 13 pages, page body + mermaid SVG verified in dist); executed as Lane B of the G2∥G3 concurrency trial.

- **Phase 21b G4 (LANE) — cross-session signals + lane inbox.** New `core/lanes/lib/signals.js`: `momentum lanes signal <id> <pause|resume|redirect|kill|message> [text]` writes typed JSON signal files under the state lock into `<git-common-dir>/momentum/lanes/<id>/inbox/NNNN-<type>.json` (monotonic 4-digit seqs that survive ack history); `momentum lanes inbox <id> [--ack <seq>|--ack-all]` lists unread oldest-first and moves acked signals to `processed/`. Written from any worktree, read from any worktree. `tests/lanes-signals.test.js` 6/6 (incl. main-worktree→lane-worktree cross-session round-trip); lanes-state + lanes-open-close 13/13 no-regression. Executed as a lane opened by the new CLI itself (D8) — live cross-lane `message` signal sent into sibling G3's inbox en route.

- **Phase 21b G3 (lane) — ambient board + landing queue.** `core/lanes/lib/board.js`: `momentum lanes` renders one line per non-closed lane (plan node, grade, status, age, ✉ unread, ⚠overlap) with the always-on queue-pressure footer; `momentum lanes queue` lists done lanes FIFO by doneAt with fresh/stale rebase-freshness vs the current worktree's HEAD; `--json` for both marked unstable (internal per ADR-0002). New `tests/lanes-board.test.js` (7/7); lanes state + open-close regression 13/13. Dogfood (D8): executed as a lane opened by the new CLI, board rendered this repo's live G3/G4 lanes including G4's ✉ signal.

- **BUG-015 fixed (lane) — commit-msg bypass accepts the house `merge: ` subject style.** Lane `fix-BUG-015-merge-style-bypass` (concurrent with a sibling bugfix lane): `COMMIT_SUBJECT_BYPASS` in `core/git-hooks/contract.js` extended with `^merge: ` (fix candidate (a) — smallest, honors all historical `merge: x → y` commits), mirrored byte-identical to the self-repo's live `.githooks/contract.js` (now tracked). `tests/git-hooks.test.js` +2: real-`git commit` acceptance of `merge: a → b (v0.25.0)` (git-default + rejection cases regression-covered) and a `.githooks`↔`core` mirror-integrity parity test. 3 adapter fingerprints re-baselined (drift = the git-hooks contract file only). Targeted 14/14; full suite 697 → 699. Record: `specs/adhoc/fix-BUG-015-merge-style-bypass/record.md`.


- **BUG-014 fixed (lane) — legacy `.momentum/` gitignore dir rule neutralized by upgrade.** `refreshGitignore()` (`bin/momentum.js`) now comments out a bare `.momentum/` / `/.momentum/` line in place (with `.bak`, dry-run aware, idempotent) alongside appending the `.momentum/*` + `!.momentum/installed.json` pair, so the Phase-20 D1 lock file becomes committable on pre-Phase-20 repos instead of staying silently ignored while upgrade reports success. 4 new tests in `tests/gitignore-refresh.test.js` (headline case proves it with real `git check-ignore`); suite 697 → 701. Record: `specs/adhoc/fix-BUG-014-gitignore-momentum-dir-rule/record.md`.

- **ENH-048 fixed (lane) — `lanes land` gate reads evidence from the LANE BRANCH; `--mark-landed` bookkeeping.** Lane `fix-ENH-048-land-gate-lane-branch` (concurrent with a sibling site-fix lane): `gateCheck` (`core/lanes/lib/land.js`) now tries `git show <branch>:specs/adhoc/<id>/record.md` (quick-task) / `…retrospective.md` + Verification Evidence section (phase) before falling back to the invoking worktree's filesystem (21b G5 uncommitted-record pattern stays landable) — a record committed on the lane branch validates AND merges with no untracked-file collision. New `lanes land <id> --mark-landed` records an out-of-band merge (requires `done` + branch already in HEAD; sets `landed`+`landedAt`, sends advisory nudges, skips turn/freshness/gate/merge; clear refusals otherwise) — no more `state.updateLane` node one-liners. This lane's own record was committed on its branch: the gate admitted the change that built it. `tests/lanes-land.test.js` +4 (headline = the exact today-failure end-to-end); targeted 10/10; full suite 703 → 707, no fingerprint drift. Record: `specs/adhoc/fix-ENH-048-land-gate-lane-branch/record.md`.

## 2026-07-02

- **Roadmap: Parallel Lanes scheduled as Phase 21** (operator decision closing status.md Next Action #9). Phase 21 = Parallel Lanes Walk / ENH-046 (target v0.23.0); Reach → 22 (v0.24.0), Intelligence → 23 (v0.25.0), Platform → 24 (v1.0, dependency-aware tasks moved out to FEAT-028). Rationale: Lanes is the launch differentiator and fixes the operator's daily pain — dogfooding compounds. Also repaired roadmap.md drift found during the edit: the file was missing the completed Phase 20 (Upgrade Hardening, v0.22.0) row entirely, still numbered Reach/Intelligence/Platform as 20/21/22 contradicting status.md, and its unscheduled table still described the Phase-8 streams work as open (closed won't-do since Phase 19/TD-008).

- **Parallel Lanes platform direction adopted** (`specs/planning/platform-parallel-lanes.md`). Operator brainstorm (SIEVE → FORGE) concluded: momentum = the **coordination & trust plane** for parallel agent development — success metric is idea-to-*trusted-merge* time; own Lanes/Waves/Board/Gates/Queue, delegate isolation substrate by detection (plain git default, treehouse/GitButler optional), never require extra installs, no daemon, no visual dashboard (publish the lane-state contract later instead). Model: one recursive plan graph (ecosystem ⊃ repos ⊃ phases ⊃ tasks), one wave engine at every scale. WIP unbounded with visible queue pressure. Filed FEAT-026/027/028 + ENH-047; ENH-046 is the arc's walk step. Native execution engine parked with written triggers. Roadmap placement (Lanes vs Phase 21 Reach) left as pending operator decision in status.md.

- **ENH-046 landscape research recorded** (`specs/planning/research-parallel-agent-landscape.md`). Operator clarified the pain includes branching strategy + agent-startup speed, and supplied treehouse (worktree pool manager) as a candidate. Sweep of the 2026 parallel-agent tooling market (worktree managers, orchestration UIs, GitButler virtual branches, Beads/Gastown, Kiro parallel tasks, arXiv 2603.21489): isolation + speed + orchestration are commoditized — momentum should own only the coordination/spec layer and integration discipline. ENH-046 scope refined accordingly (adds Rule-6 sequential-merge conventions; treehouse recommended not rebuilt).

- **ENH-046 filed — concurrent workstreams in one repo (spec-layer conventions).** Operator reported recurring pain working on multiple features / brainstorms concurrently in a single momentum project — the same complaint (near-verbatim) that opened the Phase-8 streams work on 2026-06-07, firing two of the explicit re-open triggers in `specs/planning/unscheduled-parallel-streams.md`. Analysis (SIEVE, negation-first): the TD-008 closure rationale (native `--worktree` + swarm) solved **git-state isolation** only; momentum's **spec layer is still serialized** — single Active Phase row in `status.md`, Rules 2/4/5/8 written in the singular, shared tracking files contending across branches. Per-phase artifacts are already parallel-safe by construction, so the fix is conventions + templates (multi-row Active Phase, branch-scoped rule language, brainstorms declared off-lane, append discipline for shared files), NOT a streams CLI — the TD-008 won't-do stands for the CLI. Plan: dogfood the conventions in this repo first with a pre-written escalation threshold; template-ize via a small phase + ADR if the trial holds. Verdict + full slice recorded in the ENH-046 backlog entry.

- **BUG-016 filed — `init`/`upgrade` has no ecosystem-root guard.** Investigated why a session in `../cerebrio-ecosystem/` oriented as a phase project (hunted for `specs/status.md`). Root cause: the repo's CLAUDE.md is the standard phase-project template, installed by a plain `momentum init` (2026-06-14, v0.20.2) and re-cemented by `momentum upgrade` (2026-06-28, v0.22.1) — project-mode install never checks whether the target itself contains `ecosystem.json` (sibling scan only), and the ENH-025 ecosystem CLAUDE.md is written solely at `ecosystem init` with no retrofit path for pre-Phase-15 ecosystems. Filed as P1 with fix candidates (target-self detection + `ecosystem upgrade` refreshing root instructions); live repair of `../cerebrio-ecosystem/CLAUDE.md` still needed.

- **BUG-016 resolved — ecosystem-root guard + coordination-root self-heal** (lane `fix-BUG-016-ecosystem-root-guard`). `momentum init`/`upgrade` now refuse to install the phase-project scaffold when the target itself is an ecosystem coordination root (`assertNotEcosystemRoot()`, both `name` + `members[]` required so foreign `ecosystem.json` files don't false-positive); `momentum ecosystem upgrade` now writes/repairs/refreshes the root's own CLAUDE.md/AGENTS.md before the member sweep (`refreshRootInstructions()` — retrofit for pre-ENH-025 roots, BUG-016 repair with `.bak` + `## Project Extensions` preservation, warn-not-clobber for user-owned files). 8 new tests (`tests/ecosystem-root-guard.test.js`); suite 707 → 715 green. Live `../cerebrio-ecosystem/` repaired the same day (its commit 77a0c88).

- **TD-006 resolved — evidence capture out of `npm test`** (lane `refactor-TD-006-evidence-capture`). Both swarm e2e `captureEvidence()` functions now no-op unless `MOMENTUM_CAPTURE_EVIDENCE=1`; new `npm run capture-evidence` script re-baselines the 6 committed evidence files deliberately. `npm test` is pure verification again — full suite (715/715) leaves a clean tree.

- **ENH-049 filed — `ecosystem init` should ship the coordination command surface it advertises.** Surfaced by the BUG-016 live cleanup of `../cerebrio-ecosystem/`: its `/scout`/`/dispatch`/`/session` etc. only existed by accident of the project-mode mis-install; a fresh ecosystem's CLAUDE.md promises commands that don't resolve. The cleanup's curated fileset (8 commands + 2 session scripts + SessionStart-only settings.json) is the reference shape.

- **ENH-049 resolved — ecosystem roots now ship the command surface they advertise** (lane `feat-ENH-049-ecosystem-command-surface`). `ecosystem init` (new `--agent` flag, default claude-code) installs the curated coordination fileset — `/scout /dispatch /handoff /continue /initiative /session /ecosystem /swarm`, both session scripts (exec bit), SessionStart-only `.claude/settings.json` — and `ecosystem upgrade` retrofits/refreshes it per detected adapter surface (`.claude/commands` / `.codex/commands` / `.agent/workflows`), warning about (never deleting) project-layer commands. Bonus: fixed the global `--agent` extraction swallowing the flag before ecosystem dispatch (the documented `ecosystem upgrade --agent` override never worked). 9 new tests; suite 715 → 724. Live cerebrio root validates as "11 file(s) up to date".
- **Phase 23 — Rules Unification started** (lane `phase-23-rules-unification`, runs before Phase 22 Reach). Single-source agent instructions: canonical `core/instructions/rules-body.md` generated into all three adapters' primary instruction files; `.agent/rules/project.md` retired with delete-if-pristine migration. Brainstormed + approved 2026-07-03; root cause: two-tier rule architecture left Codex/Antigravity blind to detailed rules (live Rule 6 violation same day).

- **Phase 23 — Rules Unification complete on its lane** (landing + v0.26.0 release pending approval). Single-source instruction generation shipped (ADR-0004): all three adapters' primary instruction files now carry the complete rulebook (Rules 1–15, Red Flags, anti-rationalization, Rule 13 folded in) generated from `core/instructions/rules-body.md`; `.agent/rules/project.md` retired with whitespace-tolerant delete-if-pristine migration (+ orphan-cleanup shield); ~20 reference sites repointed; fingerprints re-baselined post-audit; suite 733/733; self-repo dogfooded (its own pristine copy migrated out). ENH-050 filed en route (lanes open based this lane on the checked-out unlandable branch — mid-phase rebase required).

- **v0.26.0 released — Phase 23: Rules Unification.** Single-source instruction generation (ADR-0004); complete rulebook on every adapter's auto-loaded surface; `.agent/rules/project.md` retired with safe migration. Suite 733/733.


- **VAL-002 adjudicated — agy-wrapper closure REJECTED; stays blocked** (lane `feat-VAL-002-antigravity-compatibility`). The 2026-07-03 live-Antigravity commit 33db31a (auto-provision `agy` wrapper at `momentum init` + `sessionStartHook: true` + closure claim) was dropped on rebase onto v0.26.0 main: the wrapper validates momentum's own shim, not the vendor runtime (loads skills itself; never wires `.agents/hooks.json`; `google-antigravity`'s `hooks.policy` is permission policies, not lifecycle hooks — no SessionStart ever observed), hardcodes a `/Users/avinash` shebang in shipped code, and pip-installs an alpha library as an init side effect (no ADR). New fact recorded: `google-antigravity` (PyPI, Google LLC, alpha) is real and imports cleanly — a possible future headless-validation path. ENH-051 filed (opt-in provisioning salvage); rejected diff preserved at `specs/adhoc/feat-VAL-002-antigravity-compatibility/rejected-33db31a-agy-wrapper.patch`. Lane lands docs/tracking only.

- **BUG-017 filed — committed `.githooks/*` tracked non-executable (100644); Rule 6 enforcement silently OFF in every fresh worktree/clone.** Found when this lane's docs commit skipped commit-msg validation (`hook was ignored because it's not set as executable`). Primary worktree hooks work only via working-tree chmod; the index was never updated. Also flags committed `.bak` artifacts in `.githooks/`. Recommended: `git update-index --chmod=+x` + extend the BUG-012 exec-bit suite guard to `.githooks/`; heal-on-open folded into ENH-050.

- **BUG-017 resolved — `.githooks/` exec bits committed (100755); Rule 6 enforcement now real in lane worktrees and fresh clones.** Quick-task on lane `fix-BUG-017-hook-exec-bits`: `git update-index --chmod=+x` on all four hook files (matches `installGitHooks()`'s 0o755, so upgrades no longer dirty the tree with mode-only diffs); four tracked `.githooks/*.bak` upgrade artifacts dropped + gitignored; BUG-012 suite guard extended to `.githooks/` (100755 + no-`.bak` assertions). Heal-on-open preflight folded into ENH-050 as planned. Record: `specs/adhoc/fix-BUG-017-hook-exec-bits/record.md`.

- **Phase 24 — OKF Adoption brainstormed and planned** (lane `feat-open-knowledge-format`, `/brainstorm-phase`, gate honored). Spec pinned: Google Cloud Open Knowledge Format v0.1 (2026-06-13; `GoogleCloudPlatform/knowledge-catalog` `okf/SPEC.md`). Operator decisions: full bundle conformance; distributed frontmatter replaces `specs/phases/index.json`; phase number 24. ADR renumbered to 0005 at write time (0004 taken by Phase 23). Four phase files at `specs/phases/phase-24-okf-adoption/`.

- **Phase 24 — OKF Adoption started** (`/start-phase`, lane `feat-open-knowledge-format`). index.json + phases README + status.md Active Phase row updated. Execution: G0 → (G1∥G2∥G3) → G4 self-repo migration → G5; hard stop at merge/release gate.

- **Phase 24 — OKF Adoption complete on lane `feat-open-knowledge-format`** (v0.27.0 prepared; landing + release pending approval). specs/ = OKF v0.1 bundle everywhere: frontmatter-distributed phase metadata (index.json retired), impact-map.md, `momentum okf check|index`, idempotent upgrade migration, OKF-native templates, self-repo dogfood (192-file bundle green). Suite 733 → 769. ADR-0005; ENH-052 filed; BUG-017 discovered en route (P1, off-lane).

- **v0.27.0 released — Phase 24: Open Knowledge Format (OKF v0.1) Adoption.** Landed via `lanes land --execute` (queue head, rebase-fresh, gates green); staging fast-forwarded to main; GitHub Release + npm publish per the project release checklist. specs/ is an OKF v0.1 bundle everywhere; existing projects migrate idempotently at `momentum upgrade`.
- **Phase 22 — Reach: opencode Adapter STARTED** (`/start-phase`, lane `phase-22-opencode-adapter`, target v0.27.0). Brainstormed same day (gate contract honored): full-parity native-idiom opencode adapter, live-validated in-phase (`opencode-ai` installs via npm — no deferred VAL item). Reach re-scoped: opencode displaces Cursor/Gemini (stay P1 backlog). Execution: G0 skeleton → (G1 commands+skills ∥ G2 enforcement plugin ∥ G3 agents+spawn) → G4 wiring+regression → G5 live validation+docs+release. Prerequisite BUG-017 (hook exec bits) fixed+landed first so lane enforcement is real.

- **Phase 22 — Reach: opencode Adapter COMPLETE on lane `phase-22-opencode-adapter`; v0.27.0 built + verified (release at operator gate).** momentum's fourth adapter, full parity, and the first with in-phase LIVE validation (opencode 1.17.13, free-tier models, zero credentials): 23 native commands, enforcement plugin (gate/reminder/banner), 4 agents, native skills. Capability firsts: `skills: true` + `parallelSubagents: true` both flipped on live evidence (skill-tool load; overlapping task timestamps); `sessionStartHook` stays false (session.created unobserved in run-mode — honest fail per the VAL-002 standard). Live-found plugin bug (after-hook args) fixed in-phase via callID correlation. Core fix: installHookFiles no longer .bak's identical files (BUG-017's litter mechanism); TD-006 completed on the multi-adapter e2e. Suite 734 → 756. Roadmap drift repaired (Rules Unification row; Intelligence → 24 @ v0.28.0; Platform → 25 @ v1.0). Evidence: specs/phases/phase-22-opencode-adapter/evidence/.

- **Phase 22 opencode retargeted v0.27.0 → v0.28.0 at landing** (rebase onto post-OKF main; Rule 6 Landing Order). The OKF lane released v0.27.0 first; opencode's release commit, retrospective, roadmap and status rows now carry v0.28.0. No content changes to the adapter itself.
- **Phase 22b — Antigravity 2.0 Full Adoption started** (lane `phase-22b-antigravity-2-adoption`, parallel to phase-22 opencode; target v0.28.0). Evidence-first realignment against the shipping vendor surface (IDE 2.1.1, agy CLI 1.0.16 — installed + authenticated same day, SDK 0.1.5): G0 live-probe fact sheet → G1 hooks/paths realignment → (G2 spawn ∥ G3 native surfaces: subagent defs, plugin packaging, global-skills opt-in, doctor check) → G4 truth-sync (closes VAL-002, supersedes ENH-051, FEAT-008 closure rec — Gemini CLI sunsetted 2026-06-18) → G5 release.

- **Phase 22b G1 — Antigravity adapter realigned to the locked 2.x contract** (ADR-0006). Canonical `.agents/` root (24 workflows+engine moved; upgrade orphan-cleans legacy `.agent/`); vendor named-group hooks.json over the real five events (SessionStart removed — it does not exist); new boundary shim translates payloads/responses and delivers the handoff banner via PreInvocation `ephemeralMessage` at invocationNum 0 with a notice queue for PostToolUse reminders. Claude/Codex installs byte-identical (capture tool: "no change"); antigravity fingerprint re-baselined once. Suite 733 → 739.

- **Phase 22b G2 — swarm spawn on real agy flags.** Detached launch from repoPath with `--new-project` isolation + `--print-timeout` bound + per-repo supervisor log; fictional `--cwd`/`--skill` gone; missing-binary returns the official-installer hint. Live smoke: BOOT-OK from a real agy 1.0.16 session. Suite 740/740.

- **Phase 22b G3 — native 2.0 surfaces.** `momentum antigravity plugin-pack [--global]` packs the 5 skills as a native plugin (live `agy plugin validate` → [ok]); `momentum doctor` advises the official agy installer for antigravity projects (never provisions — ENH-051 supersession); reviewer-subagent deliverable dropped per evidence (no such vendor surface — reviewers stay skills). Suite 740 → 745.

- **Phase 22b G4 — truth-sync.** Capabilities/parity/swarm docs rewritten to evidenced reality (hooks fire-verified; `shipped-as-preinvocation` for the banner; zero stale "no CLI exists" claims); VAL-002 **resolved** with vendor-runtime evidence; ENH-051 closed-superseded; FEAT-008 closure recommended (Gemini CLI sunsetted); ENH-055 filed (deferred 2.0 surfaces).

- **Phase 22b COMPLETE ON LANE — Antigravity 2.0 Full Adoption** (landing + v0.28.0 release pending operator approval). Evidence-first rebuild vs agy 1.0.16: canonical `.agents/` root (ADR-0006), real five-event hooks via boundary shim, PreInvocation banner injection, spawn on real flags (live BOOT-OK), plugin-pack (live-validated), doctor advisory. VAL-002 resolved after 20 days blocked; ENH-051 superseded; FEAT-008 closure recommended. Suite 733 → 745; Claude/Codex byte-identical throughout.

- **Phase 22b retargeted v0.28.0 → v0.29.0 at landing** (rebase onto post-OKF/post-opencode main; Rule 6 Landing Order). Collision renumbering on rebase: ADR-0005→ADR-0006 (OKF holds 0005), my ENH-052→ENH-054 (agy hang watchdogs), ENH-053→ENH-055 (deferred 2.0 surfaces). No adapter content changes.

- **Release-race note (v0.28.0 npm)**: v0.28.0 (Phase 22 opencode) was tagged + GitHub-released, and its npm publish succeeded at 13:55 — but the artifact was removed from the registry ~13:56 during the concurrent v0.29.0 (Phase 22b) release; npm forbids reusing unpublished version numbers, so npm history is 0.27.0 → 0.29.0 (0.29.0 fully contains 0.28.0). GitHub Release annotated; git tag stands. Root cause: two live lane sessions releasing within the same minute — see BUG-018 for the sibling test-contamination cousin of this race; consider serializing releases at the runway like landings.
