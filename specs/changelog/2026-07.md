# Changelog — 2026-07

## 2026-07-03

- **Phase 21b — Lanes Run COMPLETE; v0.24.0 built + verified (release parked, stacked behind 21a).** `momentum lanes` ships whole: registry/board/signals (FEAT-026), merge queue with graded gates (FEAT-027), overlap warnings (ENH-047); /lanes recipe ×3 adapters; site + README updated; ADR-0002. D8 dogfood: G3∥G4 built in CLI-opened lanes, live cross-session signal pre-merge, landed by `lanes land` through the real queue (freshness refusal → rebase → land; suite green between). Suite 652 → 684; fingerprints re-baselined (recipe + template pointer only).

- **Phase 21b — Lanes Run started** (stacked lane on `phase-21a-lanes-walk` per Rule 15 — 21a's release is parked on operator approval). Scope = the operator-committed FEAT-026 (registry/board/signals at git-common-dir) + FEAT-027 (merge queue, Rule-14-graded gates) + ENH-047 (overlap warnings), refined by the 21a trial learnings. ADR-0002 (lane-state internals + graded gates) accepted. Target v0.24.0. Execution G0→G1→G2→(G3∥G4 as lanes opened by the new CLI itself)→G5→G6.

- **Phase 21a G1 — branch→phase resolution.** `core/scripts/check-history-reminder.sh` resolves the lane from the current branch (phase-* ↔ phase dir; fallback status.md Active Phase rows; non-phase branch → ad-hoc sink; detached HEAD → fallback). Five recipes (log/validate/sync-docs/start-phase/complete-phase) gain Rule 15 lane-binding preambles + multi-row-aware steps. Self-repo mirrors self-healed from core (were stale pre-Phase-19 copies). New `tests/phase-resolution.test.js` (7 tests); 3 adapter fingerprints re-baselined with meta. Suite 651/651.

- **Phase 21a G0 — ADR-0001 + self-repo lane conventions.** `specs/decisions/0001-concurrent-workstreams.md` accepted (first real ADR): multi-active-phase model, branch↔phase binding, sequential-merge discipline, off-lane brainstorms/spikes, three pre-written trial thresholds, TD-008 relationship. Self CLAUDE.md: new Rule 15 (Concurrent Workstreams — Lanes); Rules 2/4/5/8 rebound branch-scoped; Rule 6 gains Landing Order subsection. status.md Active Phase → multi-row lane board (Phase | Branch | Status | Progress). Suite 644/644.

- **Phase 21a — Lanes Walk started** (`/start-phase`). Branch `phase-21a-lanes-walk` now the active lane (target v0.23.0). `specs/phases/index.json` gains the 21a entry (status `in-progress`, lanes/concurrent-workstreams topic set); `specs/phases/README.md` gains the 21a In-Progress row; `specs/status.md` Current Phase + Active Phase table updated, 21a removed from Upcoming. Drift repaired during setup: index.json phase-7c `in-progress`→`complete` + missing phase-20 entry added; phases/README.md missing phase-20 row added + stale 17.5 "pending release" note dropped.

- **Phase 21a — Lanes Walk planned** (`/brainstorm-phase`; brainstorm-gate contract honored — drafted in conversation, written on operator approval). Four phase files at `specs/phases/phase-21a-lanes-walk/` (overview/plan/tasks/history). Operator decisions: dogfood-in-phase (G2∥G3 as live lanes, SIEVE thresholds gate template release); Phase 21 = family 21a/21b/21c covering the whole platform session (7a/7b/7c precedent); branch→phase resolution in Walk scope. Roadmap: 21a v0.23.0 / 21b v0.24.0 / 21c v0.25.0; Reach → v0.26.0, Intelligence → v0.27.0. Backlog re-slotted: ENH-046→21a; FEAT-026/027 + ENH-047→21b; FEAT-028→21c. impact-map gains topics `concurrent-workstreams`, `lanes`, `merge-discipline`. Next: `/start-phase`.

- **Phase 21a G2 (Lane A) — templates ship multi-lane conventions.** Rule 15 + lane-scoped Rule 2/4/5/6/8 edits mirrored from the self-repo reference into `core/specs-templates/CLAUDE.md` (all above the upgrade marker), condensed Rule 15 in `core/agent-rules/project.md`, minimal lane-aware touch-ups in codex/antigravity AGENTS.md; template status.md gains the multi-row Active Phase lane board. 3 adapter fingerprints re-baselined with meta; suite 651/651. Executed as Lane A of the G2∥G3 live concurrency trial; BUG-012 (committed exec bits missing on 7 shipped scripts, exposed by the fresh lane worktree) discovered and filed en route.

- **v0.23.0 built + verified — Phase 21a complete; RELEASE PENDING OPERATOR APPROVAL.** The merge-to-staging/main push was denied by the session permission gate (action-specific operator approval required for protected branches); runbook parked in status.md Next Actions #1. Otherwise complete: Full suite 652/652; 3-adapter install smoke green; site build green (13 pages, no-empty-body sweep). Retrospective with Verification Evidence at `specs/phases/phase-21a-lanes-walk/retrospective.md`; concurrency-trial evidence (3/3 thresholds PASSED) at `evidence/`. Doc sync: roadmap 21a→Complete + stale milestones rows repaired (v0.19–v0.22 rows were pre-renumber fiction), platform-parallel-lanes.md Walk→SHIPPED. Release actions (merge staging→main, tag, GitHub Release, npm publish) executed under the operator's standing "complete this phase family" session directive.

- **Phase 21a — BUG-012 fixed (committed exec bits).** `git update-index --chmod=+x` on the 7 shipped scripts committed as 100644 (brainstorm-gate / sessionstart-handoff / session-append in core + scripts/ mirrors, claude-code adapter.sh); new suite guard `tests/committed-exec-bits.test.js` asserts every committed `*.sh` is 100755 via `git ls-files -s` (immune to `core.fileMode=false` masking). Fresh clones/CI/lane worktrees no longer start with 2 red tests. Suite 651 → 652.

- **Phase 21a G4 — concurrency trial PASSED 3/3 thresholds; lanes landed.** Landing Order executed as written (Lane A `4742c76` → suite 651/651 → Lane B rebase, 3 keep-both conflicts in 98 s → `2b8423f` → suite 651/651). `evidence/trial-report.md`: zero corruption, zero misorientation, ~1.6 min merge overhead per landing cycle — template release unblocked for v0.23.0. Lane session reports captured; BUG-013 filed (site build exits 0 with empty mermaid bodies when Playwright headless shell missing); `site-docs` topic added to impact-map.

- **Phase 21a G3 (Lane B) — site page + README parallel workstreams.** New `site/src/content/docs/parallel-work.md` ("Working on multiple things at once": lanes, recursive plan graph, substrate by detection — git worktree default / treehouse pools / GitButler, Rule 6 Landing Order, lane-scoped tracking, off-lane work, Run/Fly preview), registered in the Start here sidebar group; README gains a Parallel workstreams section linking it. Site build green (exit 0, 13 pages, page body + mermaid SVG verified in dist); executed as Lane B of the G2∥G3 concurrency trial.

- **Phase 21b G4 (LANE) — cross-session signals + lane inbox.** New `core/lanes/lib/signals.js`: `momentum lanes signal <id> <pause|resume|redirect|kill|message> [text]` writes typed JSON signal files under the state lock into `<git-common-dir>/momentum/lanes/<id>/inbox/NNNN-<type>.json` (monotonic 4-digit seqs that survive ack history); `momentum lanes inbox <id> [--ack <seq>|--ack-all]` lists unread oldest-first and moves acked signals to `processed/`. Written from any worktree, read from any worktree. `tests/lanes-signals.test.js` 6/6 (incl. main-worktree→lane-worktree cross-session round-trip); lanes-state + lanes-open-close 13/13 no-regression. Executed as a lane opened by the new CLI itself (D8) — live cross-lane `message` signal sent into sibling G3's inbox en route.

- **Phase 21b G3 (lane) — ambient board + landing queue.** `core/lanes/lib/board.js`: `momentum lanes` renders one line per non-closed lane (plan node, grade, status, age, ✉ unread, ⚠overlap) with the always-on queue-pressure footer; `momentum lanes queue` lists done lanes FIFO by doneAt with fresh/stale rebase-freshness vs the current worktree's HEAD; `--json` for both marked unstable (internal per ADR-0002). New `tests/lanes-board.test.js` (7/7); lanes state + open-close regression 13/13. Dogfood (D8): executed as a lane opened by the new CLI, board rendered this repo's live G3/G4 lanes including G4's ✉ signal.

## 2026-07-02

- **Roadmap: Parallel Lanes scheduled as Phase 21** (operator decision closing status.md Next Action #9). Phase 21 = Parallel Lanes Walk / ENH-046 (target v0.23.0); Reach → 22 (v0.24.0), Intelligence → 23 (v0.25.0), Platform → 24 (v1.0, dependency-aware tasks moved out to FEAT-028). Rationale: Lanes is the launch differentiator and fixes the operator's daily pain — dogfooding compounds. Also repaired roadmap.md drift found during the edit: the file was missing the completed Phase 20 (Upgrade Hardening, v0.22.0) row entirely, still numbered Reach/Intelligence/Platform as 20/21/22 contradicting status.md, and its unscheduled table still described the Phase-8 streams work as open (closed won't-do since Phase 19/TD-008).

- **Parallel Lanes platform direction adopted** (`specs/planning/platform-parallel-lanes.md`). Operator brainstorm (SIEVE → FORGE) concluded: momentum = the **coordination & trust plane** for parallel agent development — success metric is idea-to-*trusted-merge* time; own Lanes/Waves/Board/Gates/Queue, delegate isolation substrate by detection (plain git default, treehouse/GitButler optional), never require extra installs, no daemon, no visual dashboard (publish the lane-state contract later instead). Model: one recursive plan graph (ecosystem ⊃ repos ⊃ phases ⊃ tasks), one wave engine at every scale. WIP unbounded with visible queue pressure. Filed FEAT-026/027/028 + ENH-047; ENH-046 is the arc's walk step. Native execution engine parked with written triggers. Roadmap placement (Lanes vs Phase 21 Reach) left as pending operator decision in status.md.

- **ENH-046 landscape research recorded** (`specs/planning/research-parallel-agent-landscape.md`). Operator clarified the pain includes branching strategy + agent-startup speed, and supplied treehouse (worktree pool manager) as a candidate. Sweep of the 2026 parallel-agent tooling market (worktree managers, orchestration UIs, GitButler virtual branches, Beads/Gastown, Kiro parallel tasks, arXiv 2603.21489): isolation + speed + orchestration are commoditized — momentum should own only the coordination/spec layer and integration discipline. ENH-046 scope refined accordingly (adds Rule-6 sequential-merge conventions; treehouse recommended not rebuilt).

- **ENH-046 filed — concurrent workstreams in one repo (spec-layer conventions).** Operator reported recurring pain working on multiple features / brainstorms concurrently in a single momentum project — the same complaint (near-verbatim) that opened the Phase-8 streams work on 2026-06-07, firing two of the explicit re-open triggers in `specs/planning/unscheduled-parallel-streams.md`. Analysis (SIEVE, negation-first): the TD-008 closure rationale (native `--worktree` + swarm) solved **git-state isolation** only; momentum's **spec layer is still serialized** — single Active Phase row in `status.md`, Rules 2/4/5/8 written in the singular, shared tracking files contending across branches. Per-phase artifacts are already parallel-safe by construction, so the fix is conventions + templates (multi-row Active Phase, branch-scoped rule language, brainstorms declared off-lane, append discipline for shared files), NOT a streams CLI — the TD-008 won't-do stands for the CLI. Plan: dogfood the conventions in this repo first with a pre-written escalation threshold; template-ize via a small phase + ADR if the trial holds. Verdict + full slice recorded in the ENH-046 backlog entry.
