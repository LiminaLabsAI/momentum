# Changelog — 2026-04

## 2026-04-21

- Project bootstrapped — spec structure initialized, Phase 0 started
- Vision, principles, and success criteria written
- Phase 0 plan: template files → install.sh → README
- **v0.1.0 released** — Phase 0 complete. 8 commands, hook script, agent rules template, install.sh, README shipped. install.sh smoke test passed.
- BUG-001 found: install.sh `realpath` blank line when target dir is new (P3, cosmetic)
- Phase 1 brainstormed and started — Tool-Agnostic Architecture: `core/` + `adapters/claude-code/` DIP restructure, `--coding-agent` flag on install.sh
- **v0.2.0 released** — Phase 1 complete. `template/` replaced by `core/` + `adapters/claude-code/`. `--coding-agent` flag shipped. BUG-001 fixed. FEAT-007–010 (Cursor, Gemini, OpenCode, Copilot) added to backlog as P2.
- Phase 2 brainstormed and started — npx CLI Distribution: `npx momentum init` Node.js CLI, zero-dependency, Claude Code only, npm publish targeting v0.3.0
- **v0.3.0 released** — Phase 2 complete. `@avinash-singh-io/momentum` published to npm. `bin/momentum.js` zero-dependency CLI ships `init` command. package name `@avinash-singh-io/momentum`.
- Phase 3 started — Gap Fixes: specs-templates scaffold, `--coding-agent` flag (TD-002), `.npmignore` (TD-001), command doc fixes (ENH-005/006/007), success message fix (ENH-004). Target v0.4.0.
- Phase 3 complete — all smoke tests passing. Discovery: `.npmignore` doesn't filter `files`-listed dirs; fixed by explicit glob allowlist in `package.json files`. v0.4.0 ready to release.
- `brainstorm-project` split → `brainstorm-idea` (exploration, no files) + `start-project` (scaffold). Mirrors brainstorm-phase → start-phase pattern.
- **v0.4.0 released** — Phase 3 complete. Full specs/ scaffold on `momentum init`. `--coding-agent` flag with adapter.js DIP. 9 commands. README rewritten.
- Phase 4 brainstormed and started — Enhanced Commands: `momentum upgrade` CLI (backup + overwrite), `/validate` (index-first + `--deep`), `/migrate` (gap fill for manual structures). Target v0.5.0.
- **v0.5.0 released** — Phase 4 complete. `momentum upgrade` CLI ships backup + overwrite conflict resolution. `/validate` command (index-first + `--deep` flag). `/migrate` command (gap detection, skip-if-exists, index reconciliation). 11 commands total.
