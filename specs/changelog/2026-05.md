# Changelog — 2026-05

## 2026-05-08

- Tracking cleanup: committed leftover Phase 3/4 polish that had never been tracked — 4 new commands (`brainstorm-idea`, `start-project`, `migrate`, `validate`), `.claude/settings.json` hook config, doc refinements to `start-phase` and `track`, semver fix in `bin/momentum.js` update check, local permission additions
- Phase 5 brainstormed — Rules & Upgrade Safety. Vision C scope (upstream improvements + persuasion-hardening of Rules 2/6/8/10/11). Targets v0.6.0
- Phase 5 started on branch `phase-5-rules-and-upgrade` — Group 0 (template foundation) blocks Groups 1-3
- ENH-008 status flipped from "deferred" to "scheduled-phase-5" — `--coding-agent` → `--agent` is a hard breaking rename consolidated into v0.6.0
- Phase 6 added to roadmap as placeholder — Execution Engine (subagents, TDD, verification rigor) — explicitly assumes Phase 5's hardened rules
- **Phase 5 implementation complete** — all six groups landed (template rules + marker logic + flag rename + dogfood + smoke tests). Five commits on `phase-5-rules-and-upgrade`. Ready for `/sync-docs` + `/complete-phase` + `npm publish`
- **v0.6.0 ready to release** — Rules & Upgrade Safety. Breaking change: `--coding-agent` flag renamed to `--agent` (old flag exits 1 with rename hint). New: Rules 10 (architecture stability, monorepo only) + 11 (evaluator discipline). Persuasion-hardening (Red Flags + anti-rationalization counters) on Rules 2/6/8/10/11. CLAUDE.md `## Project Extensions` marker — `momentum upgrade` now preserves user content byte-for-byte. Pre-marker projects auto-migrate with `.bak` backup. Naming conventions extended: `infra:` commit type, SLA column, delete-after-merge convention. Dogfooded: momentum's own CLAUDE.md migrated to marker format
- Phase 5 dogfood discovered three implementation bugs caught only by smoke testing: (1) `core/agent-rules/project.md` was missing its marker section, (2) `partitionByMarker` was lossy due to trailing-whitespace stripping, (3) `upgradeMarkedFile` produced a double-marker via redundant concatenation. All three fixed in the Group 4 commit
- Phase 6 brainstormed — Adapter Overlay & Verification. Two pillars: structural (per-agent overlays under `adapters/<name>/{commands,agent-rules,scripts}/` with conflict = error) and functional (Rule 12 verify-before-claim + `/complete-phase` evidence rigor). Proof-of-concept: `/review-code` Claude-Code-only with role-based subagents. Includes carry-forward ENH-014 (cross-repo Rule 9) and `tests/` for the CLI. Targets v0.7.0
- Roadmap extended through v1.0 — Phase 7 Execution Excellence (subagent runner, TDD opt-in, systematic-debugging, SessionStart), Phase 8 Reach (Cursor + Gemini adapters), Phase 9 Intelligence (self-learning, retrospective-driven rule evolution), Phase 10 Platform (MCP server, /specify, /decide, skill authoring). Sequencing principle: structural foundations before features that exploit them
- Phase 6 started on branch `phase-6-overlay-and-verify` — Group 0 (Adapter Contract v2) blocks Groups 1/2/3 (verification rigor in core/, Claude Code overlay /review-code, tests/ for CLI)
- **Phase 6 implementation complete** — all five groups landed (overlay + conflict detection, Rule 12 + /complete-phase evidence + cross-repo Rule 9, /review-code Claude-Code overlay, 24-test CLI suite, dogfood). Five commits on `phase-6-overlay-and-verify`. 24/24 tests pass. Ready for `/sync-docs` + `/complete-phase` + `npm publish`
- **v0.7.0 ready to release** — Adapter Overlay & Verification. New: Adapter Contract v2 (per-agent commands/agent-rules/scripts overlays under `adapters/<name>/<sub>/`, additive-only with conflict-detection that aborts before any writes). Rule 12 (Verify Before Claim) with persuasion-hardening. `/complete-phase` evidence rigor — captures stdout/stderr from validations into a "Verification Evidence" section in retrospective.md and gates the Release section behind it. `/review-code` (Claude Code) — role-based subagent reviews (security/QA/architecture) via the new adapter overlay. ENH-014: Rule 9 cross-repo safeguards in template + agent-rules + `/sync-docs`. `tests/` directory — 24 tests via Node `node:test` (no new deps), serial-mode `npm test`. Dogfood: momentum upgraded on itself; ENH-017 logged (project-name customization vs upgrade)
- Phase 6 dogfood discovered: (1) ENH-017 — project name in CLAUDE.md managed section is reset on upgrade (P2, Phase 7 candidate), (2) `.agent/rules/project.md` was pre-marker and stale (auto-migrated, then de-duped manually), (3) stale `.claude/commands/brainstorm-project.md` removed (orphan from Phase 3 rename — momentum upgrade does not delete commands)
- **v0.7.0 released** — Phase 6 complete. Branch `phase-6-overlay-and-verify` merged staging → main, tagged v0.7.0. Retrospective written with Rule-12 Verification Evidence (npm test 24/24 pass, fresh-init + conflict + dogfood smokes captured). 8 commits on the phase branch covering brainstorm → start → 5 execution groups → release prep
- **BUG-002 caught immediately post-v0.7.0-publish** — npm tarball missing `adapters/claude-code/commands/review-code.md`. The `files` glob `adapters/**/commands/` matched only the directory, not its contents. Users `npx`-installing v0.7.0 didn't get the overlay's `/review-code`. Caught by inspecting the published tarball with `tar -tf` immediately after `npm publish`. Lesson: even after Rule-12 evidence, the published artifact ≠ what the test suite verifies — `tests/` runs against local files, not the npm tarball. Future enhancement candidate: a "tarball-shape" test that runs `npm pack --dry-run` and asserts expected paths
- **v0.7.1 hotfix** — patches the BUG-002 glob: `adapters/**/commands/**` (with `**` suffix to match contents). Verified via `npm pack --dry-run`: review-code.md now in tarball
