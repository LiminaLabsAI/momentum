---
type: Backlog
---

# Backlog

> **Last Updated**: 2026-07-06 (post-Phase-25 grooming for Phase 26: ENH-060 closed by Phase 25 Founding Contract (v0.32.0); Phase 22 Reach reframe confirmed — Phase 22 shipped opencode (not Cursor), 22b Antigravity 2.0, 22c Polish, so FEAT-007 (Cursor) and ENH-009 (distribution strategy) re-slotted phase-22 → unscheduled (orphaned labels, no formal decision landed for either). FEAT-002/003/005 (Phase-1 deferrals) → closed-superseded (their P2 successors FEAT-007/008/010 exist; FEAT-003 also follows FEAT-008's Gemini-CLI sunset won't-do). Genuinely-open P1s: FEAT-007 (Cursor adapter, unscheduled — needs operator pickup). Zero open bugs; zero blockers; project on track for Phase 26 Intelligence.)

---

## Priority Levels

| Level | Meaning |
|-------|---------|
| **P0** | Critical — blocks current phase |
| **P1** | High — address in current/next phase |
| **P2** | Medium — within 2 phases |
| **P3** | Low — nice to have |

**Status**: `open` | `in-progress` | `resolved` | `deferred` | `deprecated`

---

## Bugs

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| BUG-001 | install.sh: `realpath` blank line when target dir doesn't exist | P3 | resolved | phase-1 | Fixed in Phase 1: `mkdir -p "$TARGET"` now runs before `realpath` |
| BUG-002 | v0.7.0 npm tarball missing adapter overlay files (`/review-code` not shipped) | P0 | resolved | post-phase-6 | Critical: the `files` glob `adapters/**/commands/` matched only the dir, not its contents. Users running `npx @avinash-singh-io/momentum init` from npm got no `/review-code`. Caught immediately post-publish by inspecting the published tarball. Fixed via glob change to `adapters/**/commands/**` (and `agent-rules/**`, `scripts/**`); patch-released as v0.7.1. |
| BUG-003 | `momentum init` copied macOS AppleDouble `._*` metadata files into target projects | P1 | resolved | phase-7b | Discovered during Codex install smoke on the T7 Shield worktree. `copyDir()` copied external-drive AppleDouble sidecar files from `core/` and command directories into fresh installs. Fixed by skipping `._*` and `.DS_Store` in `copyDir()` and `listFilesRecursive()`. |
| BUG-004 | Session-log concurrent-commit race in `session-append.sh` | P1 | resolved | phase-10 | Surfaced during Phase 9 review. Fix landed in Phase 10 G2: per-session-file `mkdir`-based lock (portable across macOS/Linux without `flock`). Verified via 10-process concurrent stress test. |
| BUG-005 | `findRoot` walks parents only; ecosystem CLI fails from inside a member repo | P1 | resolved | phase-10 | Surfaced during Phase 10 `/complete-phase` smoke. `momentum ecosystem status` from inside a member repo errored because `findRoot()` walks parents only while the ecosystem root is a SIBLING of members. The shell hook already scanned siblings; the JS path didn't, so ENH-021 was only half-fulfilled. Fix: `resolveEcosystemRoot` in `bin/ecosystem.js` falls back to `state.js findRegistration` (sibling-walk) when `findRoot()` returns null. Regression test in `tests/ecosystem-cli-location-agnostic.test.js`. |
| BUG-006 | `momentum init`/`upgrade` writes literal `<Project Name>` placeholder into CLAUDE.md title | P1 | resolved | 2026-06-15 | Discovered while running upgrade on the momentum self-repo (v0.14.0 → v0.14.0). Title line `# Project Rules: momentum` and intro line were inside the upgrade-managed section and got replaced with the generic template's `<Project Name>` placeholder. **Re-confirmed 2026-06-15 during cerebrio dogfood** (Phase 17.5 Next Action #1): every CLAUDE.md across 5 cerebrio member repos (ecosystem + test-lab + cli + open-guard + open-shield-python) ships the literal `# Project Rules: <Project Name>` first line because `installPrimaryInstruction()` / `upgradePrimaryInstruction()` in `bin/momentum.js` (lines 100-141, 370-376, 471-476) do byte-for-byte copy with **zero template substitution** — momentum has no template engine. Bumped P2 → P1 because the dogfood proved this affects every install at scale, and `init` itself is also affected (not just `upgrade`). Workaround: manually restore after upgrade. Fix candidates: (a) detect project name from `package.json` / dir name and substitute at install/upgrade time (recommended — minimal change in two functions), (b) treat the title block as preserved (move marker), or (c) add a `## Project Identity` section above `## Project Extensions` that the upgrade leaves untouched. |
| BUG-007 | Codex hooks `apply_patch\|shell` matcher silently misses every Bash call | P1 | resolved | post-v0.20.1 | Surfaced during 2026-06-13 audit-verification pass. Official Codex docs ([Hooks](https://developers.openai.com/codex/hooks)) state the canonical `tool_name` emitted for shell commands is `"Bash"` (PreToolUse matchers use `Bash`, `apply_patch`, `Edit\|Write`, or MCP names). Momentum's `adapters/codex/hooks.json` uses `apply_patch\|shell` — `shell` never matches in real Codex runtime, so `brainstorm-gate.sh` + `check-history-reminder.sh` are completely bypassed on every `Bash` tool call. The existing synthetic test passes only because `tests/_helpers.js::payloads.codexShell()` injects `toolName: 'shell'` itself. Fix: change matcher to `apply_patch\|Bash` in both PreToolUse and PostToolUse; rename helper to `codexBash`; reframe tests against real `tool_name: "Bash"`. Validate live once VAL-001 lands. |
| BUG-008 | `momentum init` silently overwrites `.claude/commands/*`, `scripts/*`, and `.agent/engines/*` with no backup | P1 | resolved | phase-20 | **Resolved Phase 20 (v0.22.0)** via fix candidate (c): `copyDir` gained a `backup` mode that saves `.bak` before overwriting a differing momentum-owned file; `init` passes it for commands/scripts/engines/overlay. Fresh installs stay quiet; re-init is now idempotent and safe. Tested in `tests/upgrade-safety.test.js`. — Original: Discovered 2026-06-13 during install-behavior audit. `bin/momentum.js::init()` calls `copyDir()` for slash commands (line 318), hook scripts (line 330), and execution engines (line 350) WITHOUT passing `upgradeMode` or `skipIfExists`. Existing files at those paths get overwritten plain — no `.bak`, no warning, no diff log. By contrast: `init` correctly protects `CLAUDE.md` / `AGENTS.md` (skip-if-exists), `.agent/rules/project.md` (skip-if-exists), `.claude/settings.json` (skip-if-exists with manual-merge hint), and every individual file under `specs/` (skip-if-exists per-file). The asymmetry is the bug: a user who customized `.claude/commands/dispatch.md` (or any momentum-shipped recipe), or who has a `scripts/<custom>.sh` matching a momentum-shipped filename, loses it on re-init. Reproducer: `npx @avinash-singh-io/momentum init` twice on a project where the user edited a shipped recipe — the second run silently restores the upstream version. Fix candidates: (a) treat `init` as truly fresh-only and exit non-zero if any momentum-owned target file already exists (forces users to `upgrade` instead); (b) auto-route to `upgrade` semantics for momentum-owned paths when targets exist; (c) always write `.bak` before overwriting on init the same way `upgrade` does. Recommend (b) — `init` becomes idempotent and safe to re-run. Pair with ENH-040 (`--dry-run`). |
| BUG-010 | `ecosystem upgrade --autostash` reported `failed` (not `upgraded`) on 2 large repos; re-run completes cleanly | P2 | closed | unscheduled | Surfaced 2026-06-20 during the live cerebrio fleet dogfood (v0.22.1). Sweeping `momentum ecosystem upgrade --autostash` across 7 members: 5 upgraded (sapience/frontend/infra clean-restore; py/cli upgraded with autostash conflict → work safe in stash), but **open-guard + open-shield-python reported `failed`** (lock not written) — i.e. `upgrade()` threw mid-write inside `withAutostash`, so the lock never landed and the stash pop also conflicted (work preserved in `momentum-autostash` stash). **Re-running `momentum upgrade <repo>` completed both cleanly** (lock → 0.22.1). **No data loss** — the autostash safety invariant held (work recoverable in each repo's stash). **2026-07-02 (`fix/BUG-010-sweep-error-visibility`, `specs/adhoc/BUG-010/record.md`): first half fixed** — `printSweepSummary()` in `bin/ecosystem.js` captured `err.message` per repo but never rendered it in the final summary table, only in the scrolled live log; now the `failed` summary line includes the error text inline. Regression test added (`tests/ecosystem-upgrade.test.js`, mid-write EACCES repro). Suite 640 → 644. **Still open**: the deeper root cause — why `upgrade()` threw mid-write on those 2 specific repos, and making the write-path resilient so a mid-write throw under autostash doesn't leave a half-upgrade — remains unreproduced (not reproducible synthetically per the original report; the two real repos are already past the failure at 0.22.1, so there's no live state left to diagnose against). Relates to FEAT-022. **Closed 2026-07-06 (fix/issue-sweep-v0.30.1): cannot reproduce** — 16 days, zero recurrences across dozens of upgrades incl. two self-upgrades and the v0.30.0 multi-adapter rework (which replaced the write path entirely); the two affected repos are long past the failure state. Reopen with a repro if it resurfaces. |
| BUG-011 | `init`/`upgrade` skips ALL git-hook install when `core.hooksPath` is set to anything but `.githooks` (even the default `.git/hooks`) — repos get zero enforcement while install reports success | P1 | resolved | post-v0.22.2 | Handed off from cerebrio-sapience (`.momentum/inbox/read/handoff-001.md`, 2026-06-20, phase-52). Root cause: `installGitHooks()` guard `if (huskyPresent \|\| (existingHooksPath && existingHooksPath !== ourPath))` skipped the ENTIRE install whenever `core.hooksPath != .githooks` — including the git default `.git/hooks` — without checking whether momentum's hooks were actually present, so Rule 6 lifecycle enforcement was silently absent while install/upgrade printed success. Reproduced in cerebrio-sapience (`core.hooksPath=.git/hooks`, only `.sample` files). **Resolved:** `installGitHooks()` rewritten additive/self-healing — husky stays a genuine skip; any configured `core.hooksPath` is RESPECTED and hooks installed additively INTO it (never relocated); new `installHookFiles()` installs missing hooks, upgrades momentum-owned ones (`.bak` first), leaves foreign same-named hooks untouched (warn-not-clobber via `isMomentumHookFile()`, matching current + legacy signatures so `upgrade` self-heals pre-fix repos); `._*` AppleDouble sidecars skipped (exFAT edge case). `core.hooksPath=.githooks` is set only when nothing is configured. Same function runs on both `init` (bin/momentum.js:705) and `upgrade` (:838), so self-heal is automatic. Tests: `tests/git-hooks.test.js` +4 (additive-into-configured-path with live enforcement, foreign-clobber protection, husky genuine skip, upgrade self-heal). Suite 640 → 643. Verified live via the handoff's repro command. Follow-up: momentum's OWN source repo still has no hooks (core.hooksPath unset) — `momentum upgrade .` on it would now self-install them. |
| BUG-009 | Rule 6 "Git Lifecycle (Automatic)" overstates reality — every git behavior is advised-only prose with no enforcing mechanism | P1 | resolved | phase-19 | Surfaced 2026-06-19 during a multi-agent git-lifecycle review. Rule 6's header says "(Automatic)" and its table marks branch-creation / commits / pushes / branch-deletion as `Auto? Yes`, but NONE are automated by code: `bin/momentum.js` never writes `.git/hooks/*`, never sets `core.hooksPath`, ships no husky/lefthook; the only shipped hooks (`brainstorm-gate.sh` / `check-history-reminder.sh` / `sessionstart-handoff.sh`) never touch git. The merge-to-main approval gate — the highest-stakes control — rests entirely on agent goodwill (no pre-push hook, no shipped branch protection). The `--no-verify` red-flag guards a commit-time check that does not exist. This overstatement ships verbatim to every downstream install via `core/specs-templates/CLAUDE.md`. Doc-correctness bug (Tier-A "A1"). Fix: reword "(Automatic)" → "Agent-convention (advised)" for every prose-only row and repair the incoherent `--no-verify` line. Sequencing: this is the immediate honesty fix; FEAT-018 adds the real mechanism that later re-licenses stronger wording. |
| BUG-012 | 7 shipped shell scripts committed with mode 100644 (non-executable) — fresh checkouts/worktrees fail 2 suite tests; masked locally by `core.fileMode=false` | P1 | resolved | phase-21a | Discovered 2026-07-03 by Lane A during the Phase 21a G2 live concurrency trial — the first full suite run in a fresh lane worktree. `git ls-tree -r HEAD` shows `core/scripts/brainstorm-gate.sh`, `core/scripts/sessionstart-handoff.sh`, `core/ecosystem/scripts/session-append.sh`, their three `scripts/` mirrors, and `adapters/claude-code/adapter.sh` all committed as `100644` (only `check-history-reminder.sh` ×2 and `install.sh` are `100755`). The primary working tree masks this: the repo has `core.fileMode=false` and the on-disk files carry local exec bits that git never recorded. Any fresh checkout (new clone, CI, or a Rule 15 lane worktree) materializes the committed 644 → `tests/brainstorm-gate.test.js` "hook script exists and is executable" fails (asserts owner exec bit) and `tests/ecosystem-hook.test.js` "hook: appends a commit line…" fails (`check-history-reminder.sh:83-85` invokes `session-append.sh` directly as an executable → silent no-op → no session file). Deterministic, reproduced in isolation pre-fix and green post-`chmod +x`. Directly undermines the Phase 21a+ lane-worktree flow: every new lane hits 2 red tests on an untouched tree. In-lane workaround applied: `chmod +x` on the 7 files (invisible to git under `fileMode=false`, so no commit pollution). Real fix: `git update-index --chmod=+x` the 7 files (fingerprints hash contents, not modes — but verify), consider removing/normalizing `core.fileMode=false`, and add a suite guard asserting committed modes for `*.sh` so this can't regress. **Resolved 2026-07-03 (phase-21a, post-G4):** `git update-index --chmod=+x` applied to all 7 (index now 10/10 `.sh` at 100755); new suite guard `tests/committed-exec-bits.test.js` asserts committed modes via `git ls-files -s` (immune to fileMode masking). Suite 651 → 652 green. `core.fileMode=false` left as-is (harmless once modes are committed). |
| BUG-013 | Site build silently emits EMPTY page bodies for all mermaid pages when Playwright's chromium-headless-shell is missing — build still exits 0 | P2 | resolved | post-v0.25.1 | Discovered 2026-07-03 by Lane B during the Phase 21a G3 live concurrency trial, first `npm run build` in a fresh worktree: exit 0, `13 page(s) built`, but `/parallel-work/`, `/concepts/`, `/swarm/`, `/orchestration/`, `/skills/` all shipped ~24KB shells with empty `sl-markdown-content` divs. The only error signal is a mid-log `browserType.launch: Executable doesn't exist at …/ms-playwright/chromium_headless_shell-1223/…` from mermaid-isomorphic (rehype-mermaid), which does not fail the build; empty renders are then CACHED in `.astro`/`node_modules/.astro`, so a later fixed environment can still rebuild empty until caches are cleared. Note `npx playwright install chromium` is NOT sufficient — the headless shell is a separate download (`npx playwright install chromium-headless-shell`). Risk: a fresh CI/deploy environment could silently publish a gutted site. Fix candidates: (a) a `postbuild` script that fails the build if any dist page's content div is empty (cheapest, catches every cause); (b) pin mermaid rendering strategy to error loudly (investigate rehype-mermaid error options); (c) document the two-step Playwright install in site/README + deploy workflow. Recommend (a)+(c). **Resolved 2026-07-03 (lane fix-BUG-013-postbuild-empty-body-gate)**: shipped (a)+(c) — zero-dependency `site/scripts/check-dist-content.mjs` wired as `postbuild` scans every `dist/**/index.html` and exits 1 listing any page with an empty/whitespace `sl-markdown-content` body (custom-splash landing page checked by non-trivial `<body>` text length), with the headless-shell install + `.astro` cache-clear hint; deploy workflow now runs `npx playwright install --with-deps chromium chromium-headless-shell`. Verified: fresh `npm ci && npm run build` green with the gate's ✓ line (12 pages, min body 6461 chars); synthetically gutted `concepts/index.html` caught with exit 1 naming the page. Record: `specs/adhoc/fix-BUG-013-postbuild-empty-body-gate/record.md`. |
| BUG-014 | `refreshGitignore` appends `.momentum/*` + `!installed.json` but a pre-existing directory-level `.momentum/` rule defeats the negation — lock file stays silently ignored on pre-Phase-20 repos | P2 | resolved | post-v0.25.0 | Discovered 2026-07-03 during the momentum self-upgrade to v0.25.0: `git add .momentum/installed.json` refused ("paths ignored") because the repo's legacy Phase-7a `.gitignore` line `.momentum/` (directory-level) wins over the appended negation — git cannot re-include files under an ignored DIRECTORY, so FEAT-024's additive refresh produces a pair that never takes effect when the old rule exists. Impact: every repo whose .gitignore predates Phase 20 (e.g. the cerebrio fleet) silently fails D1 (committed version-of-record) while `upgrade` reports success. Fix candidates: (a) `refreshGitignore` detects a bare `.momentum/` rule and comments it out (with `.bak`, like its other edits) before appending the pair; (b) doctor check that `git check-ignore .momentum/installed.json` resolves to the negation. Self-repo fixed manually in the v0.25.0 self-upgrade commit. Relates FEAT-024, Phase 20 D1. **Resolved 2026-07-03 (lane `fix-BUG-014-gitignore-momentum-dir-rule`, `specs/adhoc/fix-BUG-014-gitignore-momentum-dir-rule/record.md`):** fix candidate (a) implemented — `refreshGitignore()` now detects lines that are exactly `.momentum/` or `/.momentum/` (whitespace-tolerant; `.momentum/*`, negations, and comments untouched) and comments them out in place with a BUG-014 explanation, with `.bak`, dry-run intent printing, idempotent re-run, and it runs even when the modern pair is already present. 4 new tests in `tests/gitignore-refresh.test.js` (8/8), headline case proves the fix with real `git check-ignore` (lock file committable, other `.momentum/` contents still ignored). Suite 697 → 701. |
| BUG-015 | `commit-msg` hook rejects the repo's own `merge: x → y` message convention — first live hook use left a merge uncommitted | P2 | resolved | post-v0.25.0 | Discovered 2026-07-03, minutes after the self-repo's first-ever hook install (v0.25.0 self-upgrade landing): `git merge --no-ff -m "merge: chore/upgrade-self-v0.25.0 → staging"` stopped at "Not committing merge" because `COMMIT_SUBJECT_BYPASS` in `core/git-hooks/contract.js` (line 52) only exempts git's default `^Merge ` style (+ Revert/fixup/squash/amend), while the CLAUDE.md Naming Conventions and ALL of this repo's historical merge commits use lowercase `merge: x → y`. The hook and the house style have disagreed since Phase 19 shipped them — invisible until hooks were actually installed here (they enforce on every downstream repo already!). Workaround used: git-default style "Merge X into Y (…)". Fix candidates: (a) extend the bypass regex with `^merge: ` (one-line, template-wide); (b) add `merge` to the accepted Conventional types; (c) flip the house convention to git-default style. (a) is smallest and honors existing history. Relates FEAT-018, Phase 19. **Resolved 2026-07-03 (lane `fix-BUG-015-merge-style-bypass`):** fix candidate (a) — `COMMIT_SUBJECT_BYPASS` extended with `^merge: ` in `core/git-hooks/contract.js`, mirrored byte-identical to the self-repo's live `.githooks/contract.js` (now tracked). +2 tests in `tests/git-hooks.test.js`: real-`git commit` acceptance of `merge: a → b (v0.25.0)` (git-default `Merge branch 'x'` still accepted, `merged stuff` still rejected) + a mirror-integrity parity assertion. 3 adapter fingerprints re-baselined (drift = the git-hooks contract file only). Targeted 14/14; full suite 697 → 699 green. Record: `specs/adhoc/fix-BUG-015-merge-style-bypass/record.md`. |

| BUG-016 | `momentum init`/`upgrade` (project mode) has no ecosystem-root guard — running it inside a coordination root installs the full phase-project scaffolding over the ecosystem instructions | P1 | resolved | post-v0.25.2 | Discovered 2026-07-03 investigating why a session in `../cerebrio-ecosystem/` oriented as a phase project ("First file to read: ALWAYS specs/status.md" → file doesn't exist) before recovering by hand. Timeline in that repo: scaffolded 2026-06-07 pre-ENH-025, so `ecosystem init` wrote no CLAUDE.md; 2026-06-14 a plain `momentum init` (v0.20.2, commit d04cb93) ran in the coordination root and installed 29 project files (project-template CLAUDE.md with `<Project Name>` placeholder, `.agent/rules`, `.claude/commands`, `scripts/`); 2026-06-28 `momentum upgrade` (v0.22.1, 0bc8215) refreshed the same wrong template, cementing it. Two gaps: (1) `init`/`upgrade` never check whether the TARGET ITSELF is an ecosystem root — `maybePromptForAutoEcosystem` (bin/momentum.js:1335) scans SIBLINGS for `ecosystem.json` only, so the coordination root is treated as a normal project; (2) the ENH-025 managed ecosystem CLAUDE.md/AGENTS.md is written only by `ecosystem init` (bin/ecosystem.js:203-238, `renderEcosystemInstruction` has no other call site) — no retrofit path exists for pre-Phase-15 ecosystems, and `ecosystem upgrade` sweeps members but never refreshes the root's own instruction files. Net effect: every agent session in the ecosystem root loads phase-project rules, hunts for `specs/status.md`, and gets zero in-context signal about initiatives//scout//dispatch — exactly the ENH-025 failure mode, resurrected by the project-mode overwrite. Fix candidates: (a) `init`/`upgrade` detect `ecosystem.json` in the target and route to the ecosystem templates (`core/ecosystem/templates/ecosystem-{claude,agents}.md`, preserving `## Project Extensions`) instead of installing the project scaffold; (b) `ecosystem upgrade` also refreshes the root's own CLAUDE.md/AGENTS.md (self-heals pre-fix repos). Recommend (a)+(b). Live repair needed in `../cerebrio-ecosystem/` regardless — its CLAUDE.md is the project template today. **Resolved 2026-07-03 (lane `fix-BUG-016-ecosystem-root-guard`, `specs/adhoc/fix-BUG-016-ecosystem-root-guard/record.md`):** both fix candidates shipped — (a) `assertNotEcosystemRoot()` in `bin/momentum.js` runs at the top of `init()` and `upgrade()` and refuses with an actionable message when the target's `ecosystem.json` carries both `name` and a `members` array (both keys required so a foreign, e.g. PM2-style, `ecosystem.json` can't false-positive); (b) `refreshRootInstructions()` in `bin/ecosystem.js` runs at the start of every `ecosystem upgrade` — writes missing root CLAUDE.md/AGENTS.md (pre-ENH-025 retrofit), repairs a project-template file (the BUG-016 signature; `.bak` first, `## Project Extensions` user content preserved, boilerplate-only tails swapped for the ecosystem tail), refreshes stale ecosystem templates, and leaves non-momentum files untouched with a warning. 8 new tests in `tests/ecosystem-root-guard.test.js`; suite 707 → 715 green. Live `../cerebrio-ecosystem/` repaired same day (commit 77a0c88 there: rendered ecosystem CLAUDE.md + AGENTS.md; verified `= up to date` by the new self-heal). |
| BUG-017 | Committed `.githooks/*` are tracked mode 100644 (non-executable) — every fresh worktree/clone runs with the Rule 6 enforcement layer silently OFF | P1 | resolved | fix/BUG-017-hook-exec-bits | Discovered 2026-07-05 in lane `feat-VAL-002-antigravity-compatibility`: committing there produced `hint: The '.githooks/commit-msg' hook was ignored because it's not set as executable` — the commit-msg validation never ran. Root cause: the installer chmod'ed the WORKING TREE, never the index, so every `momentum lanes open` worktree and fresh clone got non-executable hooks with `core.hooksPath=.githooks` set — enforcement silently disabled exactly where lanes do their work. Same failure family as BUG-012, whose suite guard filtered on `*.sh` and never covered `.githooks/`. **Resolved 2026-07-05** (quick-task, lane `fix-BUG-017-hook-exec-bits`, record at `specs/adhoc/fix-BUG-017-hook-exec-bits/record.md`): (a) all four `.githooks/` files committed 100755 via `git update-index --chmod=+x` — also stops upgrade-induced mode-only diffs since `installGitHooks()` chmods 0o755; (c) four tracked `.bak` upgrade artifacts removed + `.gitignore` rule `.githooks/*.bak`; BUG-012 guard extended (`tests/committed-exec-bits.test.js`): all tracked non-`.md` `.githooks/` files must be 100755, no tracked `.bak`. Candidate (b) — `lanes open` preflight heals working-tree exec bits — folded into ENH-050 as designed. |
| BUG-018 | Ecosystem sibling-discovery makes "no ecosystem" tests flaky under concurrent suite runs — os.tmpdir() cross-process contamination | P2 | resolved | post-v0.27.0 | Observed 2026-07-05 while landing phase-22-opencode-adapter: full suite red on `tests/sessionstart-ecosystem-banner.test.js` ("silent when no ecosystem reachable" saw `▸ Ecosystem: phase18-b-branched-antigravity (4 members)` — a CONCURRENT session's synthetic swarm-e2e ecosystem); passed in isolation and on re-run once the other suite finished. Root cause: tests that assert ecosystem-silence create their project directly under os.tmpdir(), so every foreign `momentum-*` tmp dir is a SIBLING and the hook's sibling-walk discovers other processes' ecosystems. Fix candidates: (a) nest hermetic test projects one level deeper (tmp-root/inner/proj — one-line change per test); (b) audit all silence-asserting tests for the pattern; (c) hermetic-mode env (e.g. MOMENTUM_MAX_PARENT_WALK=0) set in such tests. Multi-session parallel lanes make concurrent suites the NORM now — this will recur. **Resolved 2026-07-06 (fix/issue-sweep-v0.30.1)**: the 'no ecosystem' assertions now isolate — nested cwd + MOMENTUM_MAX_PARENT_WALK bound in sessionstart-ecosystem-banner + state-machine tests, so sibling-discovery can never escape into os.tmpdir() where concurrent suites park fixtures. |
| BUG-019 | specs/ bundle non-conformant on main — 17 OKF violations (phase-22b files + ADR-0006 landed without frontmatter) | P2 | resolved | docs/reconcile-releases | Found 2026-07-05 by `momentum okf check` during fix/opencode-sessionstart-banner; the concurrently-landing `docs/reconcile-releases` lane fixed all 17 before this lane landed (`okf check` green at 222 files post-rebase). Residual question for grooming: the okf-check suite test did not gate the full bundle while main was non-conformant — verify the reconcile lane also closed that test gap, else split a follow-up. |
| BUG-020 | Documented multi-adapter flow is destructive: `upgrade --agent <other>` orphan-removes the previous adapter's surfaces and flips the lock | P1 | resolved | phase-22c | Found 2026-07-06 layering opencode onto the self-repo (claude-code install) per the ide-support "Multi-adapter projects" docs ("installing both is additive"). Reality: `momentum upgrade --agent opencode` orphan-removed ALL 25 claude-code managed files (.claude/commands/*, .claude/settings.json, CLAUDE.md — .bak'd) because Phase 20 orphan cleanup diffs the previous manifest (claude-code files) against the new agent's write-set, and the single `agent` lock field flips to the new agent (future upgrades then maintain only it). Recovered via git restore on the lane. **Resolved 2026-07-06 (phase-22c):** ADR-0007 restructured `installed.json` from single `agent` lock to per-agent `agents` map (`{ version, agents: { [name]: { version, files: string[] } } }`). `upgrade()` orphan cleanup scoped to the upgraded agent's prior files only — other agents' files never touched. Legacy format auto-migrates on first load; idempotent. 819/819 tests green. |
| BUG-021 | `momentum ecosystem init` scaffolds destructively into an existing project repo — REPLACES README.md and .gitignore with ecosystem-root templates | P1 | resolved | unscheduled | Found 2026-07-06 the hard way: an agent session ran ecosystem init inside the momentum repo; it overwrote the product README (214 → 34 lines) and the hardened .gitignore (killing the BUG-014 negation pair, which let runtime state get committed), and scaffolded ecosystem.json/initiatives/ at the repo root. Damage was branch-local and reverted (3cefac5); `tests/repo-integrity-guards.test.js` now pins this repo's invariants, but the CLI itself needs the guard — the inverse of BUG-016: `ecosystem init` should refuse (or require --force) when the target contains a momentum project install (.momentum/installed.json), a package.json, or an existing non-template README. **Resolved 2026-07-06 (fix/issue-sweep-v0.30.1)**: `ecosystem init` now refuses when the target contains project markers (.momentum/, package.json, specs/, .git, README.md) with a sibling-topology hint; `--force` overrides. Guard tests in tests/ecosystem-init-guard.test.js incl. README-untouched assertion. |
| BUG-022 | `momentum upgrade` strips the ecosystem pointer block from the primary instruction file | P2 | resolved | chore/self-upgrade-v0.30.0 | Found 2026-07-06 during the v0.30.0 self-upgrade: the marker-aware upgrade replaces the managed region with the pristine template, wiping the `<!-- ecosystem:begin v=2 -->` block that `init`/ecosystem-detection injects above it. Every ecosystem member repo loses its pointer on upgrade until something re-injects it. Fix: upgrade should preserve (or re-inject via the same detection path as init) the ecosystem block when the target is a registered member. **Fixed in the same lane**: upgrade() now re-injects via ecoState.findRegistration + pointerLib.ensurePointerInjected after the primary-instruction rewrite (idempotent, best-effort, dry-run-aware); regression test added (init → register → upgrade → pointer survives). |

## Features

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| FEAT-001 | Tool-agnostic core/ + adapters/ restructure (DIP) | P1 | resolved | phase-1 | Delivered in Phase 1 (v0.2.0) |
| FEAT-002 | Adapter: Cursor (`.cursor/rules/`, `.cursorrules`) | P1 | closed-superseded | phase-1 | Scope cut from Phase 1; superseded by FEAT-007 (P2). **Closed 2026-07-06 (grooming): the P2 successor FEAT-007 exists; the original Phase-1 deferral row is now redundant.** |
| FEAT-003 | Adapter: Gemini CLI (`GEMINI.md`) | P1 | closed-superseded | phase-1 | Scope cut from Phase 1; superseded by FEAT-008 (P2). **Closed 2026-07-06 (grooming): also follows FEAT-008's won't-do (Google sunset Gemini CLI for non-Enterprise users on 2026-06-18; Antigravity CLI is its successor).** |
| FEAT-004 | Adapter: OpenCode | P1 | resolved | phase-22 | Scope cut from Phase 1; superseded by FEAT-009. **Closed 2026-07-05: delivered by Phase 22 (v0.28.0) — full-parity opencode adapter, live-validated.** |
| FEAT-005 | Adapter: VS Code Copilot (`.github/copilot-instructions.md`) | P1 | closed-superseded | phase-1 | Scope cut from Phase 1; superseded by FEAT-010 (P2). **Closed 2026-07-06 (grooming): the P2 successor FEAT-010 exists; the original Phase-1 deferral row is now redundant.** |
| FEAT-006 | `npx momentum init` CLI (Claude Code only) | P1 | resolved | phase-2 | Delivered in Phase 2 (v0.3.0) as `@avinash-singh-io/momentum`; auto-detection deferred (no other adapters yet) |
| FEAT-007 | Adapter: Cursor (`.cursor/rules/`) | P1 | open | unscheduled | Rules-based, no slash commands — commands become prompt rules. **Re-slotted 2026-07-06 (grooming):** the prior `phase-22 (planned)` label is stale — Phase 22 (Reach) shipped the opencode adapter, not Cursor; no Cursor adapter phase is currently scheduled. Needs operator pickup or a future "Reach remainder" phase if/when rescheduled. |
| FEAT-008 | Adapter: Gemini CLI (`GEMINI.md`) | P1 | closed-wont-do | phase-22 (planned) | Single-file convention; workflow prompts embedded as sections **Re-slotted 2026-07-03 (grooming):** Phase 22 Reach deliverable — P2→P1 per SLA (next phase). **2026-07-05 CLOSED won't-do (operator sign-off at the Phase 22b release):** Google sunset Gemini CLI for non-Enterprise users on 2026-06-18; the Antigravity CLI is its successor (`agy plugin import gemini` migrates extensions). A Gemini CLI adapter would target a discontinued product — the Phase 22b Antigravity 2.0 adapter is the effective replacement. Recommend closing won't-do at the Phase 22b landing. |
| FEAT-009 | Adapter: OpenCode | P2 | resolved | phase-22 | Convention research done in the Phase 22 brainstorm (native idiom: commands/plugins/agents/skills). **Closed 2026-07-05: delivered by Phase 22 (v0.28.0).** |
| FEAT-010 | Adapter: VS Code Copilot (`.github/copilot-instructions.md`) | P2 | open | unscheduled | Instructions-only model; commands become inline prompt snippets |
| FEAT-018 | Git-lifecycle enforcement hooks (commit-msg + pre-push), zero-dependency, installed by `bin/momentum.js` | P1 | resolved | phase-19 | Surfaced 2026-06-19 git-lifecycle review (Tier-A "A2"). Ship plain `core/scripts/git-hooks/*.sh` and have `momentum init/upgrade` install them (via `core.hooksPath` or a `.git/hooks/` copy) — **NO husky/lefthook**, preserving the zero-dependency posture (only `fs`/`path`/`process`). `commit-msg` validates the conventional-commit prefix; `pre-push` blocks direct pushes to `main`/`staging` unless an explicit approval sentinel is present. Mirror the proven `brainstorm-gate.sh` exit-non-zero blocking pattern. Converts the two highest-severity Rule-6 controls (commit conventions, merge gate) from advised-only to enforced-hard. Once shipped, fold in BUG-009's `--no-verify` doc fix (the line then guards a real check). |
| FEAT-019 | Rule 12 verification-evidence gate — Stop/pre-tag hook blocks release without evidence | P1 | resolved | phase-19 | Surfaced 2026-06-19 review (Tier-A "A5"). VERIFY is the defining lifecycle gap: no script reads `retrospective.md`, checks for a `## Verification Evidence` section, or reads exit codes — an agent can skip verification, merge, and tag with nothing stopping it. Add a Claude Code `Stop`/`SubagentStop` hook (and Codex/Antigravity equivalents) or a pre-tag check that blocks turn-end / tagging unless the active phase's `retrospective.md` contains a non-empty `## Verification Evidence` section. Same exit-2 pattern as `brainstorm-gate.sh`. Converts Rule 12 (the most-emphasized rule) from prose to enforced-hard. |
| FEAT-020 | First-class ad-hoc work types — `/hotfix` quick-task lane + declared spike mode | P1 | resolved | phase-19 | Surfaced 2026-06-19 review (Tier-B "B1"). Momentum has no first-class path for off-phase work; the phase is the only modeled unit, so hotfixes/chores/audits get shoehorned into a `—` phase row that bypasses verification + history + doc-sync (cf. v0.20.1, v0.20.3 — both shipped with no retrospective/evidence artifact). Add explicit work TYPES, right-sizing ceremony: (1) **Phase** (existing); (2) **Quick task / bugfix** — a `/hotfix` (or `/track --ship-now`) that scaffolds a `fix/BUG-NNN` / `chore/*` branch + a minimal `specs/adhoc/<id>/` record (Current / Expected / Unchanged behavior, à la Kiro's Bugfix spec) and REUSES the Rule 12 evidence gate without a full phase scaffold; (3) **Spike / exploration** — explicitly declared, exempt from acceptance gates. Mirrors Amazon Kiro's 2026 dual-mode (Bugfix / Design-first) added because strict spec-first was "culturally heavy." Pairs with ENH-044 + ENH-045. |
| FEAT-022 | `--autostash` for `upgrade` / `ecosystem upgrade` — stash dirty tree, upgrade, restore | P2 | resolved | post-v0.22.0 | Surfaced 2026-06-20 from the cerebrio dogfood: the clean-tree gate correctly protected 6 dirty member repos, but requiring a manual commit/stash per repo is friction when the in-flight work is unrelated app code. **Resolved (v0.22.1):** added `--autostash` (mirrors `git rebase --autostash`) — `withAutostash(repoPath, fn)` in `bin/momentum.js` stashes (`git stash push --include-untracked`), runs the upgrade on the now-clean tree, then `git stash pop` to restore the work exactly as it was. Wired into single-repo `momentum upgrade --autostash` and the `momentum ecosystem upgrade --autostash` sweep (per-member; `--force` still available as the no-stash alternative). Safety invariant: on a pop that can't apply cleanly (upgrade touched a file the user also changed), the stash is NEVER dropped — work stays recoverable in `git stash list` and the CLI says so. Tests: `tests/autostash.test.js` (6, incl. the conflict-preserves-work case). Suite 630 → 636. |
| FEAT-024 | `momentum upgrade` should additively refresh an old `.gitignore` | P1 | resolved | post-v0.22.1 | Surfaced 2026-06-20 as the **root cause** of the cerebrio fleet pain: `upgrade` left an existing `.gitignore` untouched (specs skeleton is skip-if-exists), so repos created before the `._*` / `.momentum/*` rules stayed polluted. On the operator's exFAT external drive this was fatal — macOS regenerated `._*` AppleDouble sidecars on every git op and exFAT's missing exec-bit caused phantom mode changes, so git could *never* reach a clean tree and automated restore was impossible. **Resolved (v0.22.2):** `refreshGitignore()` in `bin/momentum.js` additively appends any momentum/OS rules the target lacks (`._*`, `.DS_Store`, `.momentum/*`, `!.momentum/installed.json`, `.claude/worktrees/`) under a marker, with a `.bak` — never removing user lines, idempotent, dry-run-aware, and NOT recorded as managed (never orphan-cleaned). Tests: `tests/gitignore-refresh.test.js` (4). Suite 636 → 640. (The exFAT mode-change issue is filesystem-level, out of momentum's scope — documented as: keep momentum repos on a native filesystem, or `git config core.fileMode false`.) |
| FEAT-023 | `--autostash` should auto-resolve collisions on momentum-owned files toward the upgrade | P2 | open | unscheduled | Surfaced 2026-06-20 during the live cerebrio fleet restore. When a repo's uncommitted work overlaps momentum-owned paths (`.claude/`, `.agent/`, `scripts/`, `.githooks/`, `CLAUDE.md`), `git stash pop` can't auto-restore (two versions of the same file) and stops — leaving the user a manual merge. This is the *common* case for repos carrying stale, never-committed scaffolding from an old install (4 of 7 cerebrio members). The desired behavior: detect that the colliding files are momentum-owned, keep the **upgrade's** version of those (that's the point of upgrading) and restore the user's app-code WIP, automatically — so those repos restore with no manual step. **Validated recovery strategy** (proven by hand on cerebrio-py + cerebrio-cli, where the momentum files were UNTRACKED): commit the upgrade onto a side branch (`chore/momentum-upgrade-vX`), return to the work branch, `git stash pop` cleanly (the stash's base == the branch HEAD, so no conflict), upgrade isolated + WIP restored, zero loss. **Hard case** (cerebrio-open-guard + open-shield-python, momentum files TRACKED): the side-branch trick doesn't fully clean the tree (tracked-file upgrade mods + a `.claude/projects/.../memory/` dir), so restoring needed a scoped `git reset --hard` + `git clean -- <momentum paths>` + pop — destructive, operator-gated. Implementation must: (1) pre-detect momentum-vs-app overlap before stashing and choose the right strategy; (2) prefer the side-branch path; (3) for the tracked case, do the cleanup only within momentum-owned paths and only when everything is provably recoverable (upgrade committed on the side branch + WIP in the stash). Relates to FEAT-022, BUG-010. NB: implement carefully with tests — it manipulates the user's git state, so a rushed version is worse than none. |
| FEAT-025 | `momentum init --minimal` — lightweight first-run footprint so the "it's just files" claim is literally true | P2 | open | unscheduled | Surfaced 2026-06-20 during the pre-launch landing-funnel rewrite (`feat/landing-funnel-fix`). The landing + README now claim momentum is "plain files and conventions, not a framework" and "uninstall = delete the files" — but a default `init` ships a ~22KB `CLAUDE.md` + the full 13-rule machinery + a complete specs skeleton, heavier than the pitch implies and a likely first-run bounce ("tool fatigue", the critique's barrier #2). Add `momentum init --minimal`: a reduced first-run footprint (slim instruction file + `status.md`/`backlog.md` only, with the full rule set + all commands revealed progressively / opt-in via a later `momentum upgrade --full` or similar), so the "lightweight" claim holds on day one. **Deliberately deferred as a fast-follow** — decision (2026-06-20): fix the messaging funnel first (copy + demo repo handle the "heavy" perception for launch), make the product change after. Relates to the demo-repo proof artifact + the launch workstream. |
| FEAT-021 | Versioned ordered-migration escape hatch for `momentum upgrade` (`core/migrations/vX.Y.Z.js`) | P3 | open | unscheduled | Deferred from Phase 20 (decision D4). Phase 20's lock file + orphan cleanup handle the common upgrade case (declarative file re-sync) without version-aware migration scripts, because momentum's payload is markdown/shell, not user code needing codemods (unlike Angular `ng update` / Nx `nx migrate`). But some future change will be **structural** — a file rename, a `settings.json`/`hooks.json` schema bump, a path relocation — that a copy + orphan-cleanup pass models as "delete old + add new" and loses continuity (e.g. a renamed command drops the user's `.bak` lineage, or a settings schema needs a transform not a clobber). Add an ordered-migration runner: `core/migrations/vX.Y.Z.js` modules, each exporting `up(targetDir, ctx)`; `upgrade` reads the installed version from `.momentum/installed.json`, runs every migration with version > installed in semver order, then records the new version. Model on Nx's two-phase (generate plan → run, re-runnable, one at a time). Build ONLY when a release first needs it — until then it is speculative complexity. Cited prior art: Nx migrations, Angular schematics `ng update`. |

## Tech Debt

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| TD-001 | `adapter.sh` bundled in npm package unnecessarily | P3 | resolved | phase-3 | Fixed via explicit `files` glob in package.json (root .npmignore doesn't filter files-listed dirs) |
| TD-002 | `--coding-agent` flag missing from npx CLI | P2 | resolved | phase-3 | Delivered in Phase 3 (v0.4.0) — `adapter.js` DIP pattern, adapter validation, dynamic loading |
| TD-003 | `complete-phase` skill missing `npm publish` step | P1 | resolved | phase-4 | `npm publish` was never run for v0.4.0 or v0.5.0 — users got stale v0.3.0 from npm. Fixed: `npm publish --access public` added as step 4 in `core/commands/complete-phase.md` |
| TD-004 | `docs/developer-guide.md` significantly stale | P2 | resolved | unscheduled | Discovered during Phase 7a /complete-phase. Wrong git URL (`cerebrio/momentum` should be `avinash-singh-io/momentum`); references long-defunct `template/` directory (renamed to `core/specs-templates/` in Phase 1/3); references `install.sh` testing instead of `npm test`. Phase 7a patched only the gate/T7-relevant additions; broader cleanup is out of scope for that phase. Candidate for Phase 8 or as a standalone `docs:` PR. **Resolved by earlier docs passes** (verified 2026-07-06: zero occurrences of the stale git URL, template/ path, or install.sh reference remain in docs/developer-guide.md). Row was stale. |
| TD-005 | Source tree still contains macOS AppleDouble `._*` files under adapter dirs | P3 | resolved | unscheduled | Discovered during 2026-06-12 Codex runtime-gap audit. Runtime install and `npm pack --dry-run --json` correctly skip these files (BUG-003 / tarball guard), but the repo itself still contains AppleDouble sidecars under `adapters/codex/` and nested adapter paths. This is repository hygiene debt: remove tracked/untracked sidecars, add a cleanup check if needed, and verify `tests/tarball.test.js` still proves they cannot ship. **Resolved by the v0.20.1 sweep** (verified 2026-07-06: zero `._*` sidecars in the tree; tarball guard still enforces). Row was stale. |
| TD-006 | Swarm e2e scenario evidence files rewritten on every test run (non-deterministic tmp dir) | P2 | resolved | post-v0.25.2 | Discovered 2026-06-15 during Phase 18 G0 verification (`npm test` left 6 evidence files unstaged: `specs/phases/phase-17-swarm/evidence/scenario-{a,b,c}-*.txt` + `specs/phases/phase-17-5-swarm-portability/evidence/scenario-portability-{focus,join,absorb}.txt`). Tests `tests/swarm-e2e-scenarios.test.js:156` and `tests/swarm-portability-e2e.test.js:90` call `fs.writeFileSync(evidencePath, ...)` with the body containing the random tmp dir name (`Ecosystem: momentum-test-<random6>`), so every `npm test` produces a one-character diff against the committed evidence. Fix candidates: (a) pin a fixed-name subdir under the random tmp root the way `tests/project-name-substitution.test.js` does (per the BUG-006 fingerprint deterministic pattern), or (b) strip the tmp dir name from the captured body before writing, or (c) move evidence generation out of `npm test` into a dedicated `npm run capture-evidence` script that is the only thing that touches the committed files. (c) is cleanest — `npm test` should be pure verification, not artifact regeneration. Pair with Phase 18 G3 which will add Codex + Antigravity scenario evidence files for the multi-adapter e2e — wire G3's harness to use whichever pattern lands. **Bumped P3→P2 2026-07-03 (grooming):** the churn taxed the entire Lanes family day — swept 6 noise files into the 21a G0 commit, broke a lane rebase mid-landing (dirty-worktree refusal), and forced every lane brief + conductor step to carry "never stage the evidence churn" guardrails. Fix candidate (c) (dedicated capture script; npm test purely verifies) is now the recommended shape — precedent shipped in Phase 21c (`scripts/capture-three-ideas-demo.sh`, env-gated sink). **Resolved 2026-07-03 (lane `refactor-TD-006-evidence-capture`, `specs/adhoc/refactor-TD-006-evidence-capture/record.md`):** fix candidate (c) shipped — both `captureEvidence()` functions (`tests/swarm-e2e-scenarios.test.js`, `tests/swarm-portability-e2e.test.js`) now no-op unless `MOMENTUM_CAPTURE_EVIDENCE=1`; new `npm run capture-evidence` script runs the two swarm suites with the flag set for deliberate re-baselining. Verified: plain targeted run + full `npm test` (715/715) leave zero dirty evidence files; `capture-evidence` regenerates all 6. Phase 18 G3's multi-adapter evidence harness should use the same env gate. |
| TD-007 | Clean up stale released-phase / chore / audit branches on origin | P2 | resolved | phase-19 | Surfaced 2026-06-19 review. `git branch -r` shows **16** branches still on origin for work that is merged + released: `phase-7,8,12,13,14,16,17,18` + `chore/{enh-025-cleanup, gh-release-step-in-complete-phase, migrate-to-trymomentum, phase-12-release-v0.15.0, upgrade-self-v0.14.0, upgrade-self-v0.20.1}` + `audit/{codex-runtime-gaps, codex-runtime-gaps-v2}`. Direct evidence that Rule 6's "Delete merged branch: Yes" never executes. **EXCEPTION**: `phase-8-parallel-worktrees` is unmerged (see TD-008) — do NOT delete it. Delete the rest after confirming each is fully merged into `main`. Pairs with ENH-041 + ENH-042 (automate so this debt stops recurring). |
| TD-008 | Resolve phase-8 parallel-worktree limbo + fix its false "shipped v0.11.0" retrospective | P2 | resolved | phase-19 | Surfaced 2026-06-19 review. `bin/commands/worktree-manager.js` (546 LOC + 226 LOC tests, 68 passing — create via `git worktree add`, remove via `git worktree remove`/`prune` + `fs.rmSync`) is the ONLY real worktree-lifecycle implementation and it lives only on the unmerged `phase-8-parallel-worktrees` branch, in limbo since 2026-06-07 ("Don't merge as-is" — predates the Phase 10 state machine). Its on-branch retrospective falsely claims "Phase 8 shipped v0.11.0" (verified via `git show`) — v0.11.0 was never cut; a future reader could be misled. Decide: (a) reconcile with the Phase 10 state machine and ship as the `stream` feature, or (b) formally close as won't-do and delete the branch. Either way fix the retrospective first. Related: `specs/status.md` Next Action #5; roadmap "Parallel Stream Development". |
| TD-009 | `scripts/capture-fingerprints.js` doesn't cover the opencode adapter | P3 | open | unscheduled | Found 2026-07-06 during the Limina Labs namespace move: the shared capture helper re-baselines claude-code/codex/antigravity, but opencode needs its own separate path (`MOMENTUM_RESNAPSHOT_OPENCODE=1` env opt-in inside `tests/adapter-opencode-fingerprint.test.js`). Any template change now requires two re-baseline commands and it's easy to miss the second (this session hit exactly that: suite stayed red after the helper ran). Fold opencode into the helper so one `--write` run re-baselines all four fixtures. |

## Enhancements

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| ENH-001 | `/migrate` command for existing projects | P2 | resolved | phase-4 | Delivered in Phase 4 (v0.5.0) — gap detection, skip-if-exists fill, index reconciliation |
| ENH-002 | `/validate` command to check spec structure health | P2 | resolved | phase-4 | Delivered in Phase 4 (v0.5.0) — index-first default + `--deep` full scan flag |
| ENH-003 | `momentum init` should scaffold full specs/ skeleton + CLAUDE.md template | P1 | resolved | phase-3 | Delivered in Phase 3 (v0.4.0) — `core/specs-templates/` tree, recursive `copyDir()` with skip-if-exists |
| ENH-004 | Success message after `momentum init` suggests commands that immediately fail | P1 | resolved | phase-3 | Fixed in Phase 3 — success message now shows `/brainstorm-idea`, `/start-project`, `/brainstorm-phase` |
| ENH-005 | `start-phase.md` missing explicit history.md creation step | P2 | resolved | phase-3 | Fixed in Phase 3 — note added to step 3 |
| ENH-006 | `brainstorm-project.md` missing Group Execution Pattern documentation | P2 | resolved | phase-3 | Fixed in Phase 3 — command renamed to `start-project.md`; Group Execution Pattern section added |
| ENH-007 | `/track` command should auto-decide one-liner vs detail file | P2 | resolved | phase-3 | Fixed in Phase 3 — explicit decision criteria table added to `track.md` |
| ENH-008 | Reconsider `--coding-agent` flag name — term may not be the right abstraction | P2 | resolved | phase-5 | Renamed to `--agent` in Phase 5 (v0.6.0). Hard rename, no alias — old flag exits 1 with rename hint. |
| ENH-009 | Distribution strategy: npx vs native agent-ecosystem plugins vs both | P1 | open | unscheduled | [→](details/ENH-009.md) — still blocked on having ≥1 more adapter **Re-slotted 2026-07-03 (grooming):** the long-standing deferral reason ("blocked on ≥1 more adapter") has been satisfied since v0.9.0/v0.19.0 (Codex + Antigravity shipped) — the note was stale. **Re-slotted 2026-07-06 (grooming):** the `phase-22 (planned)` label is stale — Phase 22 Reach reframe (22 opencode / 22b Antigravity 2.0 / 22c Polish) shipped adapters but did not formally decide a distribution ADR; the multi-adapter arc made "both" the de-facto answer (npx + native skills/workflows/agents). Either close as resolved-by-emergence with a retroactive ADR, or schedule a short decision quick-task in the Platform (Phase 27) window. |
| ENH-010 | CLAUDE.md upgrade-safe architecture — `## Project Extensions` marker section | P1 | resolved | phase-5 | Delivered in Phase 5 (v0.6.0) — heading-based marker (chose over HTML comments); marker-aware `momentum upgrade` preserves project extensions |
| ENH-011 | Upstream Rule 10 (additive vs decisional spec changes) + Rule 11 (evaluator discipline) | P1 | resolved | phase-5 | Delivered in Phase 5 (v0.6.0) — Rule 10 marked `(monorepo only)`; Rule 11 mandates evaluator commit before loop |
| ENH-012 | Enhanced Rule 8 — meaningful change list, `[EVALUATOR]` + `[NOTE]` types, impact-map reminder | P2 | resolved | phase-5 | Delivered in Phase 5 (v0.6.0) — triggers list, `[EVALUATOR]` type, impact-map.json check, hook script reference |
| ENH-013 | Extended naming conventions — `infra:` commit type, SLA column, delete-branch row | P2 | resolved | phase-5 | Delivered in Phase 5 (v0.6.0) — `infra:` for CI/build/deploy, SLA per priority level, delete-after-merge convention |
| ENH-014 | Multi-repo support in Rule 9 — cross-repo doc sync safeguards | P2 | resolved | phase-6 | Delivered in Phase 6 (v0.7.0) — Rule 9 multi-repo block; `/sync-docs` partitions cross-repo `Affects-specs:` paths and surfaces them informationally |
| ENH-015 | Rule 12 — Verify Before Claim (universal, not TDD-specific) | P1 | resolved | phase-6 | Delivered in Phase 6 (v0.7.0) — full hardened version in CLAUDE.md template; condensed in agent-rules/project.md |
| ENH-016 | `/complete-phase` evidence rigor — capture validation output to retrospective.md | P1 | resolved | phase-6 | Delivered in Phase 6 (v0.7.0) — Step 3 captures stdout/stderr; Step 6 appends "Verification Evidence" section; Release section gated on evidence existing |
| ENH-017 | CLAUDE.md project-name customization survives `momentum upgrade` | P2 | deprecated | unscheduled | Discovered in Phase 6 dogfood: title `# Project Rules: <Project Name>` lives in the managed section, so the user's customization (e.g., "momentum") is replaced on every upgrade. Need a mechanism — title-as-extension or a one-time-set placeholder substitution at install — that preserves the customization. Workaround today: re-edit title after each upgrade. **Deprecated 2026-07-03 (grooming):** the practical pain (title reverting to the literal `<Project Name>`) was fixed by the BUG-006 substitution (v0.20.3) — init/upgrade now render the real project name. ARBITRARY custom titles in the managed section remain unsupported by design (the section is momentum-managed; project-specific content belongs under `## Project Extensions`). No remaining work. |
| ENH-018 | Tarball-shape test — assert published npm artifact contains expected paths | P1 | resolved | phase-7b | Delivered in Phase 7b. `tests/tarball.test.js` runs `npm pack --dry-run --json`, asserts required Claude + Codex adapter paths, and blocks leaked repo/test/AppleDouble paths. `prepublishOnly` now runs `npm test`. |
| ENH-019 | GitHub Actions auto-publish on tag — `.github/workflows/release.yml` | P2 | open | phase-7b-or-standalone | Surfaced during Phase 7a v0.8.0 release: `npm publish` blocked on expired local npm session. Manual `npm login` per release is friction; also a single-point-of-failure (only the owner can release). Proposal: a GitHub Actions workflow triggered on `v*` tag push that runs `npm test`, runs the tarball-shape check (ENH-018), then `npm publish --access public` using an `NPM_TOKEN` secret. Pairs naturally with ENH-018 (tarball test runs in CI before publish). Side effect: makes "release from any machine" possible — push a tag from anywhere, CI publishes. **Downgraded P1→P2 2026-07-03 (grooming):** operator decision (2026-07-03) keeps a max-duration scoped automation token in `~/.npmrc`, removing the per-release login friction that made this P1. Still the durable fix (removes the local-token dependency entirely); revisit if the token model changes. |
| ENH-020 | Context Economy — deferred phase plan (shelf-ready) | P3 | deferred | unscheduled | Full brief at [`specs/planning/future-context-economy.md`](../planning/future-context-economy.md). Two survivors after multi-agent audit: Section Map headers (additive, Rule-1-adjacent guidance, no new Rule 13) + ecosystem `STATE.md` router (router-only, banner + path-suffix invariants prevent quotation). Lifecycle & Archival rejected entirely — premature today. Locked evaluator (`tests/benchmarks/context-economy-v1/`) committed BEFORE any work per Rule 11; quality regression vetoes optimization regardless of byte savings. Pull off shelf only when a concrete trigger fires (history >500 lines, cross-repo orient ≥3 member loads in first 20 turns, ≥50 downstream projects with reported cost concerns, second non-cerebrio ecosystem adopter). NOT time-based. |
| ENH-021 | `momentum ecosystem add/remove/status` location-agnostic | P2 | resolved | phase-10 | Surfaced during Phase 9 review. Fix landed in Phase 10 G2: walk up via `findRoot()`, fall back to sibling-walk (BUG-005 fix), accept `--ecosystem <path>` explicit override. Applies symmetrically to all three subcommands. |
| ENH-022 | `MAX_PARENT_WALK = 5` is a magic number — make configurable | P3 | resolved | phase-10 | Surfaced during Phase 9 review. `findRoot()` walks at most 5 parent directories looking for `ecosystem.json` with no documentation or override. Phase 10 fix: extract `MAX_PARENT_WALK` constant from `core/ecosystem/lib/index.js` into `core/ecosystem/lib/state.js`; honor `MOMENTUM_MAX_PARENT_WALK` env override in both JS (`state.js`) and shell (`session-append.sh`) paths; document in `core/ecosystem/layout.md` "Discovery & limits" section. |
| ENH-023 | Adapter `subagents` capability — inconsistent declaration type | P3 | resolved | phase-11 | Resolved in Phase 11 G0: every adapter declares `subagents` (and the new `parallelSubagents`) as a uniform boolean. Codex caveats moved to its `roadmap` block. `tests/adapter-capabilities-declared.test.js` enforces uniform boolean shape. Closed alongside ENH-024 in the same pass. |
| ENH-024 | Adapter `skills` / `browser` / `computerUse` — string-vs-boolean inconsistency | P3 | resolved | phase-11 | Resolved in Phase 11 G0: every adapter declares `skills` / `browser` / `computerUse` as uniform booleans (`false` today on all three shipped adapters); Codex's "future" sentinel strings moved to its `roadmap` block. Audit test now asserts the uniform shape. |
| ENH-025 | Auto-write minimal managed `CLAUDE.md` in ecosystem root on `ecosystem init` | P1 | resolved | phase-15 | Surfaced 2026-06-07 from a session in `../cerebrio-ecosystem/`. `cmdInit` (bin/ecosystem.js:113-200) writes `ecosystem.json`, `README.md`, `initiatives/`, `sessions/`, `.state/`, `.gitignore` but no `CLAUDE.md` / `AGENTS.md`. CLAUDE.md (and AGENTS.md for Codex) is the only auto-loaded agent-discovery surface at session start; without it, agents invoked inside an ecosystem repo get zero in-context signal they're in a momentum coordination layer and default to the most-familiar pattern visible in any loaded parent CLAUDE.md (e.g. updating a phase table) instead of writing an `initiatives/NNNN-<slug>.md`. Fix: have `ecosystem init` write a ~30-line momentum-managed CLAUDE.md (per adapter contract — AGENTS.md for Codex too) using the `## Project Extensions` marker pattern so it stays minimal across upgrades. Content: "you are in a coordination layer, not a project — cross-repo features → write `initiatives/<NNNN-slug>.md`; per-repo planning stays in each member; never plan implementation here" + orchestration primitives index. Companion fixes also tackled in Phase 15: action-bearing pointer block, SessionStart ecosystem banner, dispatch CLI upfront mode notice, `initiative create` CLI. |
| ENH-030 | `/complete-phase` should run `gh release create` after tagging | P1 | resolved | unscheduled | Discovered 2026-06-08 by user after Phase 13 release — both v0.15.0 and v0.16.0 were tagged (`git tag` + `git push origin <tag>`) and the npm artifacts were published, but **no GitHub Releases were created**. The tags exist at `/tags` but not at `/releases`, so the project's "Releases" page showed v0.14.0 as Latest even after two newer versions shipped. Root cause: the upstream `core/commands/complete-phase.md` template lists `git tag` + `git push <tag>` as the release step but does NOT mention `gh release create`. Fix: add an explicit "Create GitHub Release" step between tag-push and npm-publish. Steps: (a) parse the retrospective for highlights + verification + acceptance; (b) run `gh release create <tag> --title "v0.X.Y — Phase N: <name>" --notes <body> --latest --verify-tag`; (c) verify with `gh release list --limit 3`. Pair with a `release.yml` workflow (ENH-019) so manual local runs and CI converge on the same release-creation path. Affects every momentum-installed project, not just the self-repo — gap is in the global template. **Already implemented** — core/commands/complete-phase.md step 10 runs `gh release create` after tagging (verified 2026-07-06). Row predated the Phase 19-era recipe update. |
| ENH-031 | Add DevOps / Platform engineer persona to landing `<Personas />` | P2 | open | unscheduled | Surfaced during Phase 14 brainstorm. Original draft proposed adding a 4th persona ("Run agents that manage infrastructure, deployments, on-call response — same discipline that keeps a coding project honest keeps an ops agent honest") to reinforce the "momentum is not just for coding" positioning pivot. User opted to defer: "Devopersona, we will not keep it right now, we will think about it later." Natural fit when (a) we have a concrete DevOps-agent example or case study to point at, or (b) we want to visually claim that territory before a competing positioning lands there. Implementation: add a 4th card to `site/src/components/Personas.astro` with the DevOps framing; tweak grid breakpoints to accommodate (current `auto-fit, minmax(250px, 1fr)` handles 4 cards on desktop, but copy length needs to balance with the other 3). Could pair with a worked example doc later. Low risk; pure content addition. |
| ENH-032 | Member-repo pointer block is information-only, not action-bearing | P1 | resolved | phase-15 | Surfaced 2026-06-08 in the same multi-repo feedback session as ENH-025. Current pointer block (injected by `momentum ecosystem add`) is two lines: "Member of `<name>` ecosystem at `<rel>`. See ecosystem.json for siblings and `momentum ecosystem status` for live state." This tells the agent it's in an ecosystem but not WHAT TO DO when planning cross-repo work. Fix in Phase 15: bump `POINTER_VERSION` to 2; rewrite the injected block as an action-bearing 6-line stanza listing `/scout`, `/dispatch`, `/handoff`, `/continue`, plus the cross-repo routing rule ("write an initiative — never plan cross-repo features in this repo"). Existing members auto-migrate to v2 on next `ensurePointerInjected` call. |
| ENH-033 | SessionStart hook surfaces only handoffs — not broader ecosystem context | P1 | resolved | phase-15 | Surfaced 2026-06-08. `core/scripts/sessionstart-handoff.sh` (wired into Claude Code + Codex) prints pending handoff banners but is silent about ecosystem membership. An agent opening a member-repo session in a momentum ecosystem has no in-context signal of "you're in an ecosystem named X with N members and active initiative Y." Fix in Phase 15: extend the hook to walk up + sibling-scan for `ecosystem.json` (mirroring `session-append.sh` discovery), and PREPEND a 1–2 line context banner: `▸ Ecosystem: <name> (N members)` + `▸ Active initiative: <slug>` (only if `.state/active-initiative` set). Keep total hook cost <100ms. |
| ENH-034 | `momentum dispatch` CLI degrades silently to keyword-grep mode | P1 | resolved | phase-15 | Surfaced 2026-06-08. When invoked from CLI (vs. agent-driven `record()` path), `momentum dispatch` runs the in-process keyword-based `synthesizeInProcess` — there's no LLM available outside an agent harness. The fact this is happening is buried in a trailing parenthetical at the END of the synthesis block, where users have already concluded "this is the answer." Fix in Phase 15: emit a `▸ MODE NOTICE` event BEFORE the `started` event so it's the literal first line of stdout; render the same as a `> [!NOTE]` admonition immediately below the `**Mode:**` header in the artifact. Also rewrite the trailing parenthetical to a top-of-synthesis admonition. |
| ENH-035 | `momentum ecosystem initiative create` CLI never shipped (promised "Phase 9 Group 2") | P1 | resolved | phase-15 | Surfaced 2026-06-08. The `initiatives/README.md` template (`initiativesReadme()` in `bin/ecosystem.js`) says "Use `momentum ecosystem initiative create <slug>` to start a new one (coming with Group 2 of Phase 9). Until then, create files by hand following the template…" Group 2 of Phase 9 came and went; the CLI never landed. Phase 11 added the `/initiative create` SLASH command but the documented CLI subcommand still doesn't exist. Agents that correctly chose to write an initiative hit friction and hand-author the file from the template (witnessed in the dogfood session). Fix in Phase 15: wire the existing `core/ecosystem/lib/initiative.js` (`nextInitiativeId`, `writeInitiative`, `setActive`) into a new `cmdInitiative` / `cmdInitiativeCreate` in `bin/ecosystem.js`. Non-interactive (flag-driven: `--why`, `--repos`, `--owner`) so it works from any agent context without prompting. Update the README template to drop "coming soon." |
| ENH-036 | Codex command recipes should ship as native skills under `.agents/skills/` instead of `.codex/commands/` lookup fragments | P1 | resolved | post-v0.20.1 | Discovered during 2026-06-12 Codex runtime-gap audit, scoped 2026-06-13. Momentum installs 20 command recipes into `.codex/commands/*.md` and teaches AGENTS.md (lines 19–56) to look them up as plain markdown when the user types `/brainstorm-phase`. Per official Codex docs ([Skills](https://developers.openai.com/codex/skills)), the canonical project-scope reusable-workflow surface is `$REPO_ROOT/.agents/skills/<name>/SKILL.md` with `name` + `description` frontmatter — and Codex auto-discovers skills there across CLI / IDE / app. Momentum's adapter already maps `skills` to `.agents/skills/` and ships exactly one skill (`momentum-orient`); the other 20 recipes should follow the same pattern so they become real Codex skills rather than instruction-following text. (Note: custom prompts are NOT formally deprecated by Codex docs — they're just user-home scoped at `~/.codex/prompts/`, so they don't satisfy the repo-shared requirement.) Fix: convert each recipe to `.agents/skills/<name>/SKILL.md` with frontmatter; collapse the "Recipes Lookup Pattern" section in AGENTS.md to a brief skill-discovery note; update CLI success text, site docs, and parity/capability matrices. |
| ENH-037 | Refresh Codex capability matrix and docs against current Codex runtime features | P2 | open | unscheduled | Discovered during 2026-06-12 Codex runtime-gap audit on local `codex-cli 0.133.0` (latest reported 0.139.0). `codex features list` shows `hooks`, `multi_agent`, `browser_use`, `computer_use`, and plugins as enabled/stable; official docs say skills are available in CLI/IDE/app and subagent workflows are enabled by default. Momentum still declares Codex `skills: false`, `browser: false`, `computerUse: false`, and `parallelSubagents: false`, while docs carry outdated notes about `features.hooks` opt-in. Re-audit on the latest Codex CLI, update `adapters/codex/adapter.js`, `core/adapter-capabilities.md`, `core/adapter-parity-matrix.md`, site docs, and tests. **Downgraded P1→P2 2026-07-03 (grooming):** gating signal is external (requires a live/current `codex` CLI environment) — same rationale as the VAL-001 P1→P2 downgrade in v0.20.4. |
| ENH-038 | Codex hook config needs live-runtime trust and matcher validation | P2 | open | unscheduled | Discovered during 2026-06-12 Codex runtime-gap audit. Static/fake hook tests pass, but official Codex docs say project hooks require project trust plus per-hook trust review, and current docs use `hooks` as the canonical feature key (`codex_hooks` deprecated). Momentum should verify real tool event names/matchers on current Codex (`Bash` vs `shell`, `apply_patch` behavior, SessionStart startup/resume matching), document `/hooks` trust flow in AGENTS.md/site docs, and make `momentum doctor` detect untrusted or disabled Codex hooks where possible. **Downgraded P1→P2 2026-07-03 (grooming):** externally gated on live Codex runtime access, same as ENH-037/VAL-001. |
| ENH-039 | Codex swarm parity wiring remains absent despite platform-agnostic `core/swarm/` | P1 | resolved | phase-18 | Discovered during 2026-06-12 Codex runtime-gap audit and already called out in status as Phase 18. v0.20.0 ships `/swarm` only for Claude Code; Codex has no `.codex/agents/swarm-supervisor.toml`, no conductor recipe/skill, and no MCP/cwd shim to guarantee per-supervisor cwd pinning. Users on Codex can use ecosystem/scout/dispatch/handoff today but not sustained multi-repo swarm delivery. Implement in Phase 18 and add live Codex evidence before flipping the parity matrix cell. |
| ENH-040 | `momentum init --dry-run` to preview file actions before writing | P2 | resolved | phase-20 | **Resolved Phase 20 (v0.22.0).** `--dry-run` added to both `init` and `upgrade` (and `ecosystem upgrade`): a module-level `_dryRun` gates every fs-mutation + post-copy chmod/readdir + adapter direct writes + git-hook config + lock-file write + orphan removal; prints a `✋ would …` action set and exits 0. Verified writes-nothing across all 3 adapters in `tests/dry-run.test.js`. — Original: Discovered 2026-06-13 during install-behavior audit (paired with BUG-008). Today there is no way for a user to see what `momentum init` (or `momentum upgrade`) would do on their existing project without actually running it. For projects that already have `.claude/commands/*`, `scripts/*`, custom agent rules, or partial momentum scaffolding from an earlier version, the only way to find out is to run + git-diff or run + recover from `.bak` files. Worse, on init there are no `.bak` files for slash commands or scripts (BUG-008). Fix: add `--dry-run` to both `init` and `upgrade` that walks the same code paths but only prints the planned actions (`+ added`, `↑ upgraded`, `↻ migrated`, `⚠️ skipped`, `❌ would clobber without backup`) and exits 0 without touching the disk. Pair with BUG-008 so the dry-run can warn loudly about clobber-without-backup cases. Lightweight to implement: thread an `opts.dryRun` boolean through `copyDir`, `copyFile`, `applyOverlay`, and the adapter's `runInstall`/`runUpgrade`; gate every `fs.copyFileSync` / `fs.writeFileSync` / `fs.chmodSync` behind it. |
| ENH-041 | Forge-neutral server-side branch-protection guidance (NOT GitHub-specific code) | P1 | resolved | phase-19 | Surfaced 2026-06-19 review (Tier-A "A3"). **Reframed 2026-06-19 during Phase 19 brainstorm** per the vendor-agnostic / DIP principle: the original "ship a `gh api` GitHub Ruleset template into core" would hard-couple momentum to GitHub, when downstream users may be on GitLab (protected branches), Bitbucket (branch permissions), Gitea/Forgejo, or a bare remote. Resolution: the REAL enforcement stays in vendor-neutral git hooks (FEAT-018/019 — pure git, any forge, any agent), which covers momentum's actual threat model (a drifting agent, not a malicious human running `--no-verify`). ENH-041 becomes a **forge-neutral docs section** ("Optional hardening: enable your forge's server-side branch protection — GitHub Rulesets / GitLab protected branches / Bitbucket permissions — as an unbypassable backstop"). Forge-specific scaffolding, if ever wanted, belongs behind a future "forge adapter" abstraction (same DIP pattern), explicitly out of scope. |
| ENH-042 | Automated merged-branch deletion in `/complete-phase` + session-start branch-hygiene self-audit | P2 | resolved | phase-19 | Surfaced 2026-06-19 review (Tier-A "A4-code"). Today `complete-phase.md:101` only PRINTS a reminder to `git push origin --delete`; nothing runs it (→ 16 stale branches, TD-007). Add a guarded delete step gated on confirmed merge + clean `gh release`, plus a session-start self-audit (analogous to the existing tag-vs-release audit) that compares released phases in `status.md` against live origin branches and surfaces released-but-undeleted ones. Prefer GitHub's server-side auto-delete (ENH-041) as primary; this is the local/fallback path. |
| ENH-043 | gitignore `.claude/worktrees/` + document momentum doesn't manage ephemeral agent worktrees | P3 | resolved | phase-19 | Surfaced 2026-06-19 review (Tier-A "A6"). `.gitignore` ignores `.momentum/` but NOT `.claude/worktrees/` (`git check-ignore .claude/worktrees` exits 1) — the Claude Code ephemeral worktree this review session ran in is untracked only by luck, not design. One-line defense-in-depth: add `.claude/worktrees/` (and other agent-worktree paths) to `.gitignore` in the repo and the template, and note in docs that momentum does not GC Claude Code worktrees. |
| ENH-044 | Make lifecycle plumbing phase-optional + represent ad-hoc/patch releases | P2 | resolved | phase-19 | Surfaced 2026-06-19 review (Tier-B "B2"). `/log`, `/sync-docs`, the PostToolUse history hook, `/complete-phase`, and `/validate` all hard-bind to `specs/phases/<active-phase>/`, so off-phase work loses history/doc-sync and `/validate` FAILS the legitimate "no active phase" state. Fixes: (1) make `/log` + `/sync-docs` + the history hook phase-optional with a fallback sink (e.g. `specs/adhoc/<id>/history.md` or a global log); (2) add an "Ad-hoc / Patch Releases" section to `status.md` + template and retroactively move the two `—` rows (v0.20.1, v0.20.3) there; (3) teach `/validate` that "no active phase" is a valid mode, not an error. Pairs with FEAT-020. |
| ENH-045 | Rule 14 — escalation criteria for when ad-hoc work must become a phase | P2 | resolved | phase-19 | Surfaced 2026-06-19 review (Tier-B "B3"). The project repeatedly decided hotfix-vs-phase by gut (audit → v0.20.1; BUG-006 displacing Phase 18 → v0.20.3) with no documented threshold. Add a Rule 14 to CLAUDE.md (+ template) defining when a quick task must escalate to a full phase: e.g. touches > N files, modifies `specs/architecture/`, requires an ADR, changes a public contract, or displaces a planned phase. Include red-flag counters and the governing principle "select the lightest work type that fits; escalate only when scope/risk/cross-cutting impact justifies it" (per Anthropic's "simplest thing first"). Pairs with FEAT-020 + ENH-044. |
| ENH-046 | Concurrent workstreams in one repo — multi-active-phase conventions (spec layer) | P1 | resolved | phase-21a | Surfaced 2026-07-02: operator reports recurring pain working on multiple features/brainstorms concurrently in a single momentum project — the same pain that opened the Phase-8 streams work (closure record `specs/planning/unscheduled-parallel-streams.md` quotes the identical 2026-06-07 complaint), and two of that record's explicit re-open triggers have now fired. The TD-008 closure rationale (native Claude Code `--worktree` + swarm) only covers **git-state isolation**; the **spec layer is still serialized**: `status.md` models exactly one Active Phase, Rules 2/4/5/8 are written in the singular ("the active phase"), and shared tracking files (status/backlog/changelog) contend across concurrent branches. Key insight: per-phase artifacts (`specs/phases/<N>/tasks.md`, `history.md`) are ALREADY parallel-safe by construction — the serialization is narrative/convention, not mechanism. SIEVE verdict (2026-07-02): pursue a **conventions + template** slice, NOT a streams CLI — (1) `status.md` Active Phase table → N rows (phase, branch, status); (2) rule language rebound branch-scoped ("the phase bound to your branch" instead of "the active phase"); (3) declare brainstorms/spikes off-lane, zero tracking contention (`brainstorm-idea` already writes no files — say so loudly); (4) append-discipline + merge guidance for backlog/changelog contention; (5) docs page "working on multiple things at once" (worktrees for git isolation + these conventions for tracking). Dogfood the conventions in the momentum repo itself FIRST (Learn→Measure→Build); escalation threshold, written before the trial: tracking corruption, >15 min/week merge overhead, or an agent session misorienting onto the wrong phase → only then design mechanism (rebuild streams fresh per the closure record, via ADR). Template-izing touches core rule semantics = public contract → Rule 14: small phase + ADR when it ships. **2026-07-02 landscape research** (`specs/planning/research-parallel-agent-landscape.md`): operator clarified the pain includes **branching strategy** (multiple branches, no momentum-aware merge discipline) and **speed** (cold worktree startup). Market stratified into 5 layers; isolation (native worktrees/GitButler) + speed (treehouse worktree pools) + orchestration UIs are commoditized — recommend, don't build. arXiv 2603.21489 validates tracked-document coordination as THE async-agent success factor = momentum's layer; nearest competitor Beads/Gastown (DB-shaped, heavy). Scope refined: ADD integration-discipline conventions (Rule 6 extension — sequential merge, one branch at a time, rebase the rest; stacked guidance for dependent phases); towncrier-style tracking-file fragments named as the threshold-gated mechanism candidate; docs page recommends treehouse/native worktrees for the speed half. **2026-07-02 platform brainstorm:** adopted as the **walk step** of the Parallel Lanes platform arc (`specs/planning/platform-parallel-lanes.md`). Design constraints added there: conventions must be written lane-/branch-scoped so FEAT-026 lanes bind onto them without rework; WIP unbounded (no lane cap) with queue pressure surfaced instead. Relates: TD-008, FEAT-020, ENH-044, ENH-045, FEAT-026/027/028, ENH-047. **Resolved 2026-07-03 — shipped in v0.23.0 (Phase 21a Lanes Walk):** ADR-0001, Rule 15 + lane-scoped rule edits, multi-row Active Phase board, branch→phase resolution; dogfood trial passed 3/3 pre-written thresholds. |
| FEAT-026 | Single-repo Lanes — registry, ambient board, signals | P2 | resolved | phase-21b | Filed 2026-07-02 platform brainstorm (`specs/planning/platform-parallel-lanes.md`, **run** step). A **lane** = worktree + branch + bound plan node (phase or task) + agent session — the "virtual developer" unit giving one human real-team semantics in one setup. State lives ONCE per repo in plain untracked human-readable files anchored at the shared git dir (`git rev-parse --git-common-dir` resolves from any worktree): lane registry, per-lane manifest, inbox/signals — reuses swarm's proven files-as-channels + inbox protocol, re-anchored single-repo; NO daemon. Ambient visibility: `momentum lanes` renders the board from any session/worktree; agents answer "what's running?" by reading it. Interaction: signal files (pause/message/redirect/kill) writable from any session; lane agents read inbox at checkpoints. Board always shows **queue pressure** (done-but-unlanded lanes + wait time) — WIP is unbounded by design, back-pressure visible. Substrate delegated by detection: plain `git worktree` default (zero extra installs); treehouse pools detected → speed; GitButler documented. v1 = CLI/markdown; lane-state file format stays INTERNAL until dogfood-stable, then published as a small contract (the arc's one one-way door). Depends on ENH-046. Relates FEAT-027, FEAT-028, ENH-047. **Resolved 2026-07-03 — shipped in v0.24.0 (Phase 21b Lanes Run):** `momentum lanes` registry/board/signals at git-common-dir, exactly per this spec; mechanism dogfooded on its own phase (D8). Battle-tested same day on two real parallel-bugfix rounds (v0.25.1, v0.25.2). |
| FEAT-027 | Merge queue with graded evidence gates | P2 | resolved | phase-21b | Filed 2026-07-02 platform brainstorm (**run** step). `main` is the runway: lanes land ONE at a time — sequential merge, suite green, remaining lanes rebase onto updated main (consensus practice per landscape research). Extends the Phase-19 enforcement layer (pre-push sentinel + hooks) rather than new infra: per-lane merge approval, queue order honoring wave dependencies, and **Rule-14-graded gate depth** — a spike lane lands with near-zero ceremony, a `main` landing requires full Rule-12 evidence; small-diff + green-evidence lanes earn light review (this is how N parallel lanes stay reviewable by one human). Precedent that this must be mechanism, not prose: Rule 6 as advisory-only ran at 0% compliance until Phase 19 shipped hooks. Open design: rebase automation vs advisory; stacked (dependent) lane support. Relates FEAT-026, ENH-046. **Resolved 2026-07-03 — shipped in v0.24.0:** `lanes land` with FIFO turn, never-forceable rebase-freshness, Rule-14-graded gates; ENH-048 (v0.25.2) upgraded the gate to read evidence from the lane branch. Open designs (rebase automation, stacked support) remain future work under the arc. |
| FEAT-028 | Recursive wave planner — one dependency engine at every scale | P2 | resolved | phase-21c | Filed 2026-07-02 platform brainstorm (**fly** step). One plan graph — ecosystem ⊃ repos ⊃ phases ⊃ tasks — where parallelism is legal at any node whose children are independent; waves computed per level from dependency annotations; sequential landing at each integration point. Adds dependency annotations to `tasks.md` (and phase-level dependencies), computes fan-out/waves, and **absorbs both the Phase 23 "dependency-aware tasks" deliverable (intra-phase parallelism, Kiro-wave-style) and swarm's `ecosystem.json` wave ordering** into a single core engine — swarm becomes the top-scale consumer. Enables the target UX: N ideas → N planned lanes → governed parallel execution → sequenced landings. Relates FEAT-026/027, ENH-046, Phase 23, `core/swarm/`. **Resolved 2026-07-03 — shipped in v0.25.0 (Phase 21c Lanes Fly):** one recursive engine in `core/waves`, swarm rewired byte-stable as top-scale consumer, `(deps: …)`/index.json annotations, `momentum waves` CLI, 3-ideas e2e demo. Lane-state contract deliberately kept internal (ADR-0003 §5). |
| ENH-047 | Plan-time touch-path overlap warnings (semantic-conflict prevention) | P3 | resolved | phase-21b | Filed 2026-07-02 platform brainstorm. Worktrees prevent file collisions, not logic collisions — the market's unsolved gap (only Clash attempts it, by watching files after the fact). momentum-native answer: each lane's plan **declares intended touch-paths at bind time**; momentum warns on overlap between active lanes BEFORE any agent runs — conflict prevention via declared intent in specs (arXiv 2603.21489: "shared understanding through tracked documents"), zero daemons. Advisory, never blocking. Relates FEAT-026, ENH-046. **Resolved 2026-07-03 — shipped in v0.24.0:** `--touches` declarations at open; advisory overlap warnings at open/board/land. Advisory-only held up in both live parallel-fix rounds. |
| ENH-048 | `lanes land` evidence gate should read the record from the LANE BRANCH, not the invoking worktree; add a post-merge `landed` bookkeeping path | P2 | resolved | post-v0.25.1 | Filed 2026-07-03 after the first real parallel-bugfix landing (BUG-014 ∥ BUG-015 lanes). Two frictions: (1) the quick-task gate checks `specs/adhoc/<id>/record.md` in the CWD worktree, but a record committed on the lane branch only arrives WITH the merge — validating requires copying it in as untracked, which then collides with the incoming tracked file at merge time ("untracked working tree files would be overwritten"). Fix: gate reads `git show <lane-branch>:specs/adhoc/<id>/record.md` (and phase-grade retrospectives likewise). (2) When the merge is executed out-of-band (e.g. after a collision workaround), there is no CLI to mark the manifest `landed` — had to call `state.updateLane` via node. Fix: `lanes land --mark-landed-only` or auto-detect an already-merged lane branch and offer bookkeeping. Relates FEAT-027, ADR-0002 §4. **Resolved 2026-07-03 (lane `fix-ENH-048-land-gate-lane-branch`, `specs/adhoc/fix-ENH-048-land-gate-lane-branch/record.md`):** (1) `gateCheck` now reads evidence from the LANE BRANCH first — `git show <branch>:specs/adhoc/<id>/record.md` (quick-task) and `git show <branch>:specs/phases/<ref>/retrospective.md` incl. the Verification Evidence section check (phase) — with detail "(read from the lane branch)"; filesystem fallback keeps the 21b G5 uncommitted-record pattern landable. (2) `lanes land <id> --mark-landed` records an out-of-band merge as bookkeeping: requires status `done` + `merge-base --is-ancestor <branch> HEAD`, sets `landed`+`landedAt`, sends the usual advisory rebase nudges, skips turn/freshness/gate/merge; clear refusals otherwise. Headline test proves the exact today-failure end-to-end (record committed only on the lane branch → gate passes → `--execute` merges with no untracked-file collision). Targeted 10/10; full suite 703 → 707, no fingerprint drift. |
| ENH-049 | `ecosystem init`/`upgrade` should ship + manage the coordination-root command surface the ecosystem CLAUDE.md advertises | P2 | resolved | post-v0.25.2 | Surfaced 2026-07-03 during the BUG-016 live cleanup of `../cerebrio-ecosystem/`. The ecosystem CLAUDE.md/AGENTS.md templates advertise slash-command primitives (`/scout`, `/dispatch`, `/handoff`, `/continue`, `/initiative`, `/session`, plus `/ecosystem`, `/swarm`) — but `ecosystem init` ships ONLY the two instruction files; it installs no `.claude/commands/`, no `scripts/session-append.sh`/`sessionstart-handoff.sh`, no SessionStart hook wiring. The cerebrio coordination root only had a working command surface by ACCIDENT of the BUG-016 project-mode mis-install (curated by hand in its cleanup commit: 8 commands + 2 session scripts + SessionStart-only settings.json). Any fresh ecosystem gets a CLAUDE.md that promises commands which don't resolve. Fix: define the curated coordination-root fileset (commands subset + session scripts + minimal `.claude/settings.json`), have `ecosystem init` install it and `refreshRootInstructions()`/`ecosystem upgrade` (BUG-016 machinery) manage it on upgrade, per-adapter via the adapter contract (Codex/Antigravity equivalents). Keep the anti-pattern rule: no phase/project commands in the coordination root. Relates ENH-025, BUG-016. **Resolved 2026-07-03 (lane `feat-ENH-049-ecosystem-command-surface`, `specs/adhoc/feat-ENH-049-ecosystem-command-surface/record.md`):** `ensureRootCommandSurface()` in `bin/ecosystem.js` installs/refreshes the curated fileset — 8 coordination commands (5 adapter-overlay: scout/dispatch/handoff/continue/swarm, resolved adapter-first with core fallback; 3 core: ecosystem/initiative/session), 2 session scripts (exec bit enforced), and Claude Code SessionStart hook wiring (`core/ecosystem/templates/ecosystem-settings-claude.json`, written only when absent — settings are user-owned once present). `ecosystem init` gained `--agent <name>` (default claude-code; validated against `adapters/`); `ecosystem upgrade` manages every agent surface detected in the root (`detectRootAgents` via per-adapter commands destination: `.claude/commands`, `.codex/commands`, `.agent/workflows`) and retrofits the default when none exists. Refresh follows house semantics (`.bak` + overwrite for differing momentum-owned files); project-layer commands are warned about (BUG-016 anti-pattern), never shipped, never deleted. Bonus fix: momentum.js's global `--agent` extraction silently swallowed the flag before `runEcosystem` dispatch — explicitly-given `--agent` is now re-injected, fixing the documented-but-dead `ecosystem upgrade --agent` override too. 9 new tests (`tests/ecosystem-root-surface.test.js`) + 1 guard-test assertion modernized; suite 715 → 724 green. Live validation: `ecosystem upgrade --dry-run` on `../cerebrio-ecosystem/` reports the hand-curated surface as "11 file(s) up to date" — byte-identical to what the machinery now ships. |
| ENH-050 | `momentum lanes open` defaults `--from` to the invoking worktree's HEAD — silently stacks new lanes on whatever branch is checked out | P1 | resolved | unscheduled | Discovered 2026-07-04 during Phase 23: the phase lane was opened while the primary worktree sat on an unlanded (and unlandable) feature branch, so the lane silently based itself on that branch — inheriting its entire diff, its 5 failing environment-dependent tests, and a contaminated line in the phase's source extraction. Caught when the full suite surfaced the foreign failures; required a mid-phase `git rebase --onto main <bad-base>` plus fragment decontamination (see phase-23 history 2026-07-04). The Rule 6 freshness gate would have refused the landing, but only hours later. Fix candidates: (a) default `--from` to the repo's default branch (origin/main), making stacked lanes opt-in via explicit `--from <parent>`; (b) loud warning + confirmation when the implicit base isn't the default branch; (c) record the base in the lane manifest and flag off-main bases on the board. Recommend (a)+(c). **Resolved 2026-07-06 (fix/issue-sweep-v0.30.1)**: (a) new lane branches now base on the integration branch (origin/HEAD → main fallback) instead of the parked checkout's HEAD, reported in the open output; `--from` still overrides for deliberate stacking. (b) `land` refuses to merge a lane onto its own branch (self-landing observed live twice on 2026-07-06). Both regression-tested. |
| ENH-051 | ~~Opt-in `agy` wrapper provisioning~~ — SUPERSEDED: the official agy CLI exists; momentum never provisions | P3 | closed-superseded | unscheduled | Filed 2026-07-05 from the VAL-002 adjudication (lane `feat-VAL-002-antigravity-compatibility`). Rejected commit 33db31a auto-provisioned an `agy` CLI wrapper at `momentum init --agent antigravity` — right idea (the `google-antigravity` PyPI library by Google LLC is real, alpha, and imports cleanly on 2026-07-05), wrong shipping shape: init-time network side effect (venv + pip into `~/.gemini/antigravity/bin/`), hardcoded `/Users/avinash` shebang in shipped code, unearned capability flip. Salvage shape: explicit opt-in command (e.g. `momentum antigravity provision-agy`), portable shebang/templated venv path, zero capability flips — `sessionStartHook` stays gated on VAL-002 evidence. Precondition: vendor library exposes lifecycle hooks (today `hooks.policy` is permission policies only — allow/deny/ask_user). Preserved diff: `specs/adhoc/feat-VAL-002-antigravity-compatibility/rejected-33db31a-agy-wrapper.patch`. Relates VAL-002. **2026-07-05 CLOSED-SUPERSEDED (Phase 22b):** Google ships the official `agy` CLI (v1.0.16, official installer) — no wrapper is ever needed. What survives: `momentum doctor` detects a missing `agy` in antigravity projects and prints the official installer command (never provisions); `momentum antigravity plugin-pack` covers the distribution use-case natively. |
| ENH-052 | Site page: "Your specs are an open knowledge bundle" (OKF v0.1) | P2 | open | unscheduled | Filed 2026-07-05 by Phase 24 (OKF adoption). Phase 24 shipped README + developer-guide coverage only — the public-site page was deliberately deferred because the `feat-site-redesign` lane owns the site surface (Rule 15 overlap avoidance; see phase-24 history [NOTE] 2026-07-05). Scope: a docs page covering the OKF bundle layout (`type` frontmatter taxonomy, distributed phase metadata, generated index.md listings), `momentum okf check|index`, the upgrade-time migration, and interop framing (any OKF consumer can read momentum specs). Signal sent to the site lane at Phase 24 landing. Relates ADR-0005. |
| ENH-053 | `lanes done` should offer a re-grade path when a lane's work outgrew its opening grade | P3 | open | unscheduled | Registry fix 2026-07-05: the OKF lane referenced ENH-053 in `specs/adhoc/feat-open-knowledge-format/record.md` ("lane re-grade gap" — a quick-task lane that grows to phase scale has no `lanes` mechanism to re-grade; the OKF lane was re-registered by hand) but the backlog row was never filed, leaving a dangling ID. Row added by the backlog-hardening follow-up lane so the shared registry has no holes. See the OKF record for the full context. |
| ENH-054 | Headless `agy` integrations need hard external watchdogs + pacing — vendor CLI hangs intermittently with hooks.json present | P2 | open | phase-22b | Filed 2026-07-05 from Phase 22b G0 probes (agy 1.0.16): hook-ful headless runs hung indefinitely 6/10 times, IGNORING `--print-timeout`, zero log output; hook-less runs never hung; same configs passed minutes before/after — vendor hook-runner bug suspected (upstream report drafted in G4). Momentum consequences: (a) `adapter.spawn()` and any CI probe must wrap `agy` in an external kill-timer with backoff/pacing between session starts (bound into G2 design); (b) hang signature = empty `--log-file`; (c) re-probe deny-semantics + shipped-shape questions when vendor stabilizes. Evidence: `specs/phases/phase-22b-antigravity-2-adoption/evidence/fact-sheet.md` §9–10. Relates VAL-002, ENH-051. |
| ENH-055 | Adopt deferred Antigravity 2.0 surfaces: scheduled tasks, Stop-hook verification nudge, plugin `agents`/`commands` subdirs, `.agents/rules/*.md` triggers | P3 | open | unscheduled | Filed 2026-07-05 (Phase 22b G4). Surfaces observed but deliberately deferred: (a) scheduled tasks + voice (I/O 2026 features, no momentum mapping yet); (b) `Stop` hook `{"decision":"continue"}` could enforce Rule 12 ("don't stop with unverified claims") natively; (c) `agy plugin validate` recognizes undocumented `agents` + `commands` plugin subdirs (`specs/phases/phase-22b-antigravity-2-adoption/evidence/plugin-pack.md`) — potential native recipe packaging beyond skills; (d) `.agents/rules/*.md` with `trigger: always_on|model_decision` as a granular rules surface. Evidence: `specs/phases/phase-22b-antigravity-2-adoption/evidence/fact-sheet.md`. Relates ENH-054. |
| ENH-056 | `lanes land --execute` silently "lands" a lane into itself when invoked from the lane's own worktree | P2 | open | unscheduled | Filed 2026-07-05 from the Phase 22b landing train: `--execute` merges into the CURRENT branch, so running it inside the lane worktree produced `landed on 'phase-22b-antigravity-2-adoption'` — a no-op self-merge that still flipped the manifest to `landed` (repaired by hand via `state.updateLane`, twice). Fix: refuse `--execute` when `current branch == lane branch` (or when the invoking worktree IS the lane's worktree) with a hint to run from the integration branch; optionally add `--into <branch>` as an explicit escape. Relates FEAT-027, ENH-048. |
| ENH-057 | Serialize RELEASES at the runway like landings — the release race burned npm 0.28.0 permanently | P2 | open | unscheduled | Filed 2026-07-05: two live sessions released within one minute; npm 0.28.0 was published then unpublished mid-race and npm forbids version reuse — the registry now jumps 0.27.0 → 0.29.0 forever. Landings serialize via the lanes queue; releases have no equivalent mechanism (each session independently tags/publishes). Fix candidates: (a) a release queue/lock in the lanes state dir (`momentum lanes release <id>` claims the next version at the runway); (b) `/complete-phase` re-reads the registry version at publish time and refuses stale bumps; (c) at minimum, document one-release-at-a-time + verify-surfaces-before-next in the Project Extensions checklist. Relates ENH-030, FEAT-027. |
| ENH-058 | opencode: wire the ecosystem session-log + ecosystem context banner through the momentum plugin | P3 | resolved | feat/opencode-first | Filed 2026-07-05 (fix/opencode-sessionstart-banner). The plugin covers gate/reminder/handoff-banner; the two remaining shipped-degraded¹⁹ parity cells (ecosystem SessionStart context banner, auto session log) still route through `/session` + the momentum CLI. Wiring them needs payload-compatible calls into core/ecosystem/scripts semantics from plugin JS. Also note the upstream candidate: the generic `event` bus hook hangs `opencode run` on 1.17.13 (reproduced twice; probe evidence in the adhoc record) — consider filing against sst/opencode. |
| ENH-059 | npx runs a stale GLOBAL momentum even with a pinned version spec — strengthen docs + doctor guidance | P2 | open | unscheduled | Found 2026-07-06 during the self-upgrade dogfood: with global momentum at 0.27.0 on PATH, `npx -y @avinash-singh-io/momentum@0.29.0` executed `/opt/homebrew/bin/momentum` (0.27.0) from inside the repo — the pinned spec was silently ignored (traced via NODE_OPTIONS require-shim; fresh npx cache did NOT fix it; registry tarball verified correct). The README only warns about stale npx CACHE + @latest; a stale global install shadows even pinned specs. Fix candidates: (a) README/docs: recommend keeping the global fresh (`npm i -g @latest`) and note the shadow; (b) `momentum doctor` + more CLI entry points surface the existing update-available banner (lanes board already does); (c) upgrade prints a warning when the running CLI is older than the registry latest. |
| ENH-060 | Foundation-docs contract — new-project flow leaves vision/roadmap as template placeholders; no command fills, gates on, or validates them | P1 | resolved | phase-25 | Found 2026-07-06 via live dogfood on a fresh v0.31.2 project (`~/Workspace/Projects/password-manager`, opencode adapter): operator ran `momentum init` → `/brainstorm-idea` (password-manager MVP) → the agent planned Phase 1 directly and ran `/start-phase` — the entire MVP was then built and merged while `specs/vision/project-charter.md`, `principles.md`, `success-criteria.md`, and `specs/planning/roadmap.md` remained byte-identical init templates (`<Project Name>`, `YYYY-MM-DD`, `_(TBD)_`), and `status.md` kept placeholder Summary/Upcoming/Key Decisions (despite decision-heavy work: zero-knowledge crypto, PBKDF2/AES-GCM, JWT sessions). Four root-cause gaps, all in shipped surfaces: (1) **routing dead zone** — `/brainstorm-idea` closes with "run `/start-project`", but `start-project.md` declares "Run this on a NEW or EMPTY repository" and its steps are create-from-scratch (mkdir + create all files + initial commit), so on an init-scaffolded repo an agent reasonably judges it inapplicable and skips straight to phase planning; nothing anywhere says "on an init'd repo, /start-project FILLS the placeholder templates"; (2) **no foundation gate** — `/brainstorm-phase` step 2 reads `roadmap.md` but doesn't say what to do when it's a placeholder; `/start-phase` setup reads only status.md + backlog — neither stops on unfilled foundation docs; (3) **`/validate` blind spot** — checks structure (files exist, frontmatter, tables) but never placeholder content, so the broken state validates green; (4) **no fill-me signal in templates** — init's placeholder files carry no machine-checkable marker distinguishing "never filled" from "filled". **Design direction (operator, 2026-07-06): clean redesign, NOT a patch** — no fill-mode branches, no placeholder-marker heuristics bolted onto commands. Principle: **foundation docs are authored, not scaffolded** — a placeholder document is worse than no document (satisfies every structural check while carrying zero information; absence is the clean machine-checkable signal). Shape: a **project lifecycle contract** with three states, one owner each: (1) **Installed** (`momentum init`) = machinery + only the contentless-by-nature skeleton (backlog/changelog/decisions/phases/adhoc/status) — init STOPS shipping the four foundation placeholders (`vision/project-charter.md`, `vision/principles.md`, `vision/success-criteria.md`, `planning/roadmap.md`); `status.md` template gains an explicit "Not founded — run `/brainstorm-idea` → `/start-project`" state so Rule 1 orientation routes every agent correctly for free; (2) **Founded** (`/start-project`, reframed from "scaffold a NEW or EMPTY repo" to "found the project") = authors charter/principles/success-criteria/roadmap + status Summary from the brainstorm under the existing gate+approval contract, and plans Phase 0/1 — works on any repo state because its job is content, not structure (kills the init/start-project overlap debt from ENH-003 era at the same time); (3) **Phase loop** — `/brainstorm-phase`/`/start-phase` gate on Founded = foundation files EXIST (pure file-existence check, no content heuristics), route to `/start-project` when missing. `/validate` checks the invariant "phases exist ⟹ founded". Legacy migration: `momentum upgrade` detects foundation docs byte-identical to shipped templates (hash-exact, provably zero user content) → removes them + reports "not yet founded", returning old installs to the clean signal. Decisional change (init output shape + command semantics) → ADR + small phase per Rules 10/14; adapter fingerprints re-baseline. |
| FEAT-011 | `momentum upgrade` subcommand — update commands + managed CLAUDE.md with `.bak` backups | P1 | resolved | phase-5 | Delivered in Phase 5 (v0.6.0) — marker-aware `upgradeMarkedFile`; four states (added/updated/unchanged/migrated); pre-marker projects auto-migrate with `.bak` |
| FEAT-012 | Adapter Contract v2 — overlay model for per-agent commands/rules/scripts | P1 | resolved | phase-6 | Delivered in Phase 6 (v0.7.0) — additive-only overlay; CLI walks `core/` then `adapters/<chosen>/`; conflict detection exits non-zero before any writes |
| FEAT-013 | `/review-code` command (Claude Code) — role-based subagent reviews | P1 | resolved | phase-6 | Delivered in Phase 6 (v0.7.0) — Claude-Code-only overlay; dispatches three parallel Task subagents (security/QA/architecture) returning Critical/Important/Minor findings |
| FEAT-014 | `tests/` directory for momentum CLI | P2 | resolved | phase-6 | Delivered in Phase 6 (v0.7.0) — 24 tests via Node `node:test`; covers install, upgrade, marker logic, overlay walk, conflict detection. `npm test` runs serially. |
| FEAT-015 | Adapter Contract v3 — runtime metadata for multi-agent compatibility | P1 | resolved | phase-7b | Delivered in Phase 7b. Adapters declare display name, destinations, primary instruction file, config/hook files, and capability flags. Core remains generic; agent-specific capabilities stay isolated in adapters. |
| FEAT-016 | Adapter: Codex (`AGENTS.md`, `.codex/hooks.json`, command recipes) | P1 | resolved | phase-7b | Delivered in Phase 7b. First non-Claude adapter. Installs Codex primary instructions, Codex hook config, generic specs/scripts, and Codex-owned command recipes via `momentum init/upgrade --agent codex`. |
| FEAT-017 | Cross-agent install/upgrade regression coverage | P1 | resolved | phase-7b | Delivered in Phase 7b. Tests prove Claude Code output remains unchanged while Codex install/upgrade works. Includes dynamic available-agent behavior and overlay conflict protection. |

## Validation (post-release follow-ups)

> Status values: `open` (not started), `in-progress`, `partial` (some questions closed, others pending), `blocked` (closure depends on an external structural factor — e.g. an upstream feature flip or operator IDE access), `resolved`.

| ID | Title | Priority | Status | Phase | Detail |
|----|-------|----------|--------|-------|--------|
| VAL-001 | Live `codex` CLI dogfood — Phase 18 partial closure, full closure pending operator IDE session | P2 | partial | phase-18 + post-v0.20.4 | **2026-06-15 Phase 18 G4 partial closure** at `specs/phases/phase-18-swarm-parity/evidence/val-001-codex.txt` against `codex-cli 0.133.0` (binary at `/Applications/Codex.app/Contents/Resources/codex`). Closed: (a) hooks default-on confirmed (`hooks: stable: true`); (c) `multi_agent: stable: true` + 4 TOML subagents installed; (e) `skill_mcp_dependency_install: stable: true` + 21 skills installed. Open: (b) live `apply_patch|Bash` matcher firing; (d) parallel fan-out — **explicitly gated on `enable_fanout: stable: true` upstream** (currently `under development: false`); (f) live `/skill` routing. Closure path: operator runs `/swarm start` inside a momentum-installed project from the Codex desktop app, captures screen recording or terminal log, attaches to this entry. Capability flip decision (in v0.20.4): Codex `parallelSubagents` stays `false` until `enable_fanout` becomes stable upstream. Bumped P1 → P2 because the gating signal is now external (upstream Codex feature), not momentum-internal. |
| VAL-002 | Live `agy` CLI dogfood — RESOLVED by Phase 22b with vendor-runtime evidence | P2 | resolved | phase-18 + post-v0.20.4 | **2026-06-15 Phase 18 G4 evidence** at `specs/phases/phase-18-swarm-parity/evidence/val-002-antigravity.txt`. Key finding: **no standalone `agy` CLI binary exists**. The `/Applications/Antigravity IDE.app/` bundle ships `language_server_macos_arm` + `sandbox-wrapper` only; the `agy` npm package (`agy@0.0.0`, 217 B) is a placeholder; `@google/antigravity` 404s on npm. Antigravity is an IDE-only product today. Closed via file presence: workflow at `.agent/workflows/swarm.md`, supervisor skill at `.agents/skills/swarm-supervisor/SKILL.md`, hooks at `.agents/hooks.json` with documented matchers. All 6 VAL-002 questions require operator-manual validation inside the Antigravity IDE — momentum's CLI cannot exercise the IDE-side discovery or hook-firing. Closure path: operator opens a momentum-installed project in Antigravity IDE, runs `/swarm`, observes whether the agent picker / hook output appears; attaches recording to this entry. Capability flip decision (in v0.20.4): Antigravity `sessionStartHook` stays `false` until operator evidence confirms IDE surfacing. Bumped P1 → P2 because the gating signal is structurally tied to the IDE — not momentum-internal. **2026-07-05 adjudication (lane `feat-VAL-002-antigravity-compatibility`): closure claim REJECTED — stays blocked.** A 2026-07-03 live Antigravity session authored commit 33db31a closing VAL-002 via a momentum-auto-provisioned `agy` wrapper (venv + `pip install google-antigravity` into `~/.gemini/antigravity/bin/` as a `momentum init` side effect; `sessionStartHook` flipped `true`). Rejected because the wrapper validates momentum's own shim, not the vendor runtime: it loads skills itself and never wires `.agents/hooks.json`; no SessionStart event was ever observed (the library's `hooks.policy` module is permission policies — allow/deny/ask_user — not lifecycle hooks); the shipped code hardcoded a `/Users/avinash/...` shebang; and a network pip install of an alpha library at init is a decisional change with no ADR. Phase 23 independently purged the unearned "fully supported" SessionStart claim from the antigravity surfaces (90229b6). **New fact:** `google-antigravity` now EXISTS on PyPI (author Google LLC, Development Status: Alpha) and imports cleanly — updating the 2026-06-15 "no CLI path at all" finding and opening a possible future headless-validation path once the library exposes lifecycle hooks (salvage tracked as ENH-051). Rejected diff preserved at `specs/adhoc/feat-VAL-002-antigravity-compatibility/rejected-33db31a-agy-wrapper.patch`. Closure path unchanged: operator-manual IDE validation; `sessionStartHook` stays `false`. **2026-07-05 RESOLVED (Phase 22b G0):** the official `agy` CLI shipped v1.0.16 on 2026-07-02 (installed via the official installer, auth shared with the IDE) — the "structurally blocked, IDE-only" premise dissolved. All 6 questions answered with live vendor-runtime evidence: workflows register as slash commands (incl. planted markers), skills discovered, PreToolUse/PostToolUse fire with captured payloads, SessionStart DOES NOT EXIST (five-event surface; PreInvocation `ephemeralMessage` injection is the vendor-native equivalent — ADR-0006). Full fact sheet: `specs/phases/phase-22b-antigravity-2-adoption/evidence/fact-sheet.md` §8; captured payloads under `specs/phases/phase-22b-antigravity-2-adoption/evidence/hook-captures/`. `sessionStartHook` capability stays `false` pending the live injection round-trip (ENH-054 hang blocks the re-probe). |
| VAL-003 | Antigravity IDE + Agent Manager observation pass (~10 min, operator) | P3 | open | phase-22b follow-up | Filed 2026-07-05. Phase 22b live-verified the whole contract on the agy CLI; the IDE (2.1.1) and Agent Manager share the same engine per vendor docs (fact-sheet §11) but lack direct observation. Checklist (momentum-installed project, e.g. this repo): (1) open in Antigravity IDE → type `/` → confirm momentum workflows (scout, swarm, review-code…) appear in the picker; (2) ask "what skills do you have" → momentum's 5 skills listed; (3) make any file edit → confirm no hook errors in the IDE console (hooks.json parse); (4) with a `.momentum/inbox/handoff-*.md` present, start a fresh conversation → does the handoff banner surface? (5) repeat 1–2 in the Agent Manager. Attach observations to this entry. Expected outcome: confirmations only — layout is engine-shared; any deviation reopens as a bug. Relates VAL-002, ENH-054. |
