| BUG-002 | v0.7.0 npm tarball missing adapter overlay files (`/review-code` not shipped) | P0 | resolved | post-phase-6 | Critical: the `files` glob `adapters/**/commands/` matched only the dir, not its contents. Users running `npx @avinash-singh-io/momentum init` from npm got no `/review-code`. Caught immediately post-publish by inspecting the published tarball. Fixed via glob change to `adapters/**/commands/**` (and `agent-rules/**`, `scripts/**`); patch-released as v0.7.1. |
| BUG-030 | Cross-repo landing gate never fires in a real sibling layout — `findRoot` walks up only, so `lanes land` silently skips the whole ecosystem check | P1 | resolved | phase-31c | **v0.41.0 shipped ENH-068 — its headline deliverable — non-functional.** Surfaced 2026-07-27 while checking whether `findRoot`'s up-only semantics were even worth an operator question. `core/ecosystem/lib/landing.js:82` calls `lib.findRoot(repoRoot)` with **no fallback**, and `core/lanes/lib/land.js` calls `landingCheck(repoRoot)` with **no injected root**. `findRoot` walked UP ONLY, but `core/ecosystem/layout.md` documents the ecosystem root as a **SIBLING** of its members — which is exactly what `ecosystem init` + `ecosystem add ../repo` produce. So in the standard layout `findRoot` returns `null` → `landingCheck` returns `applicable: false` → the entire dependency-ordered landing gate is skipped, silently and fail-open. Reproduced live: `findRoot(frontend) = null`; `landingCheck (no root) = applicable: false`; `landingCheck (explicit) = applicable: true`. **Every Phase 31b test passed `ecosystemRoot` explicitly**, so not one exercised the discovery path production uses. `bin/ecosystem.js` and `bin/swarm.js` escaped the same bug only because each bolted on its own `state.findRegistration` fallback. **Same shape as BUG-028 and BUG-029** — the test exercised a path production does not take. 31b even shipped a matcher-reachability test specifically to close that class, and the class reproduced twice within hours; hence 31c's enumerative production-call-path guard (ADR-0018 R6) rather than another parity fence. **Fix (Phase 31c G0, ADR-0018 R3):** ONE `findRoot` — up-walk → sibling scan → registration fallback, `MOMENTUM_MAX_PARENT_WALK`-bounded, memoised. All 7 call sites migrated; `events.js`'s 31b-era local resolver is now a thin delegation; both CLIs' redundant fallbacks removed. Regression test drives the **production call path** (`landingCheck(dir)` with no root) and was demonstrated failing against the pre-31c implementation. A self-cleaning allowlist test now fails if an eighth discovery implementation appears. Also updated a pre-existing test that had **codified the bug as intended behaviour**, comment and all. Ships in v0.42.0. |
| BUG-031 | Swarm wave runner is dead code — `pollTurn` + `recordRepoComplete` have zero production callers, and wave 2+ is never spawned | P1 | resolved | epic-0001 / phase-32d | Filed 2026-07-27 during the Epic-0001 design review. **The `--mode autopilot` headline feature has no production entry point.** Three independent confirmations: (1) `pollTurn` (`core/swarm/conductor.js:305`) contains the entire autopilot wave-advance branch (`:376`) and is referenced ONLY at its definition, its export, and in `tests/swarm-start-claude-code.test.js` + `tests/swarm-e2e-multi-adapter.test.js` — `bin/swarm.js` never calls it and there is **no `poll` subcommand** among the 20 dispatched verbs. (2) `recordRepoComplete` (`:277`) — same, definition + export only. (3) `buildSpawnDirectives` is called exactly once, at `bin/swarm.js:170`, hardcoded `waveIndex: 1` — nothing ever builds directives for wave 2+. Net effect: `swarm start --spawn` launches wave 1, supervisors dutifully write saga records to `<repo>/.momentum/runs/dispatch-run-NNN.json` per `core/swarm/supervise.md:33`, and **nothing ever reads them back**; `board.json` freezes at wave-1-start permanently and `swarm status` renders the stale cache. Even with `pollTurn` wired, autopilot would flip wave 2 to `running` and stall for want of a spawn path. Undetected because the e2e tests bypass the CLI and drive the reconciler in-process — Phase 18's own history entry says so: *"drives the rest of the swarm to completion via the in-process simulator (recordRepoComplete + pollTurn)"*. **This is the BUG-028/029/030 class**; v0.42.0's enumerative production-call-path guard does not cover `core/swarm/`. **Disposition (Epic-0001 D13): do not repair.** Even fully wired, the wave runner drives one phase per repo, which the epic's tier-parameterized runner supersedes. Deprecate in 32a, replace in 32d, remove only once the replacement demonstrably covers it. `core/swarm/inbox.js` (the park primitive) and `core/swarm/lib/wave-ordering.js` (already a thin adapter over `core/waves`) are **retained**. |
| BUG-033 | v0.43.0 shipped the governor **inert in every installed project** — `hook.js` was not in the runtime closure | P0 | resolved | post-v0.43.0 | Found 2026-07-28 by installing the **published v0.43.0 tarball** into a fresh repo and running the hook: `run-governor.sh` exited **0** where it must exit **2**. Cause: `core/scripts/run-governor.sh` invokes `core/run/lib/hook.js` by PATH, and the runtime-closure walker follows only STATIC requires — so the script shipped to every install and the file it invokes did not. Net effect: **the governor, the headline feature of Epic 0001, silently did nothing anywhere except the momentum repo itself.** Fail-open by design, so nothing errored; sessions simply ended normally and no one would have learned why. This is the **third** instance of this exact shape in one epic (`pollTurn`/BUG-031, `grant.js` caught in 32b, now `hook.js`) and the second in the same `ENTRY_POINTS` list. **Fix:** `run/lib/hook.js` added to the closure (pulls `governor.js` transitively — now 5 run files vendored, was 3). **Guard:** `tests/shipped-runtime.test.js` now *enumerates* every `.momentum/runtime/…` path referenced from any `core/scripts/*.sh` and fails when one is not vendored, counting both shipping routes (closure + EXTRAS). Adding a script that reaches a new file by path now fails in the same commit. **Verified:** repacked, reinstalled from the tarball, `run-governor.sh` → **exit 2** with the continuation delivered. No test caught this; only installing the published artifact did. |
| BUG-034 | Governor **dead on Antigravity** — project root resolved from `PWD`, but Antigravity hooks run with cwd `.agents/` | P0 | resolved | post-v0.43.1 | Found 2026-07-28 while closing the VAL items, by invoking the hook **the way Antigravity actually invokes it** — cwd `.agents/`, no `*_PROJECT_DIR` exported. `run-governor.sh` resolved `root="${MOMENTUM_PROJECT_DIR:-${CLAUDE_PROJECT_DIR:-$PWD}}"`, so it looked for `.agents/.momentum/run.json`, found nothing, and **exited 0 silently**. Claude Code worked only because it exports `CLAUDE_PROJECT_DIR`; Antigravity exports neither, and every Antigravity hook command is spelled `bash ../scripts/…` precisely because its cwd is `.agents/`. **Fourth instance of this class in one arc** (BUG-002 tarball glob, BUG-030 findRoot, BUG-031 pollTurn, BUG-033 unvendored hook.js) and the second in the governor itself. **Fix:** resolution order now env → **the script's own location** (`<repo>/scripts/run-governor.sh`, so `$(dirname $0)/..` is the repo root, verified by the presence of `.momentum/`) → `PWD` last. The script no longer needs the host's cooperation, which is the same literal-path discipline as ADR-0018 R2. **Guard:** `scripts/verify-published.sh` now loops **both interceptor adapters** and invokes the hook from each one's REAL hook cwd with the env deliberately unset — v0.43.1's smoke covered claude-code only, which is how a one-adapter pass read as parity. Verified: exit 2 on both. |
