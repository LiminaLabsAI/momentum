| TD-009 | `scripts/capture-fingerprints.js` doesn't cover the opencode adapter | P3 | open | unscheduled | Found 2026-07-06 during the Limina Labs namespace move: the shared capture helper re-baselines claude-code/codex/antigravity, but opencode needs its own separate path (`MOMENTUM_RESNAPSHOT_OPENCODE=1` env opt-in inside `tests/adapter-opencode-fingerprint.test.js`). Any template change now requires two re-baseline commands and it's easy to miss the second (this session hit exactly that: suite stayed red after the helper ran). Fold opencode into the helper so one `--write` run re-baselines all four fixtures. |
| TD-012 | Shipped-runtime duplication — 3 self-contained hook helpers now mirror core modules because installs get no `core/` | P2 | open | phase-31b | Surfaced 2026-07-27 during Phase 31b G2. An installed momentum project receives **no copy of `core/`** (a fresh install ships 4 files into `.githooks/` and a handful into `scripts/`), so anything a hook needs must travel with the hook. Three files are now deliberate self-contained duplicates of core logic: **`core/git-hooks/eco-event.js`** (31a — mirrors `core/team/lib/fragments` + `core/identity`), **`core/ecosystem/lib/orient.js`** (31b G1 — inlines `resolveMemberLocation`), and **`core/ecosystem/lib/cross-repo.js`** (31b G2 — mirrors `core/ecosystem/lib/detect.js`). Each is node-builtins-only, ships beside its hook, and is fenced by a parity test asserting byte- or behaviour-identical output against the core implementation. **The discipline is holding** — no drift has occurred, precisely because the parity tests exist — but three instances is a pattern rather than an incident, and the resolution path is getting more expensive each time. Additionally, the resolver in each is ad-hoc: `cross-repo.js` carries a 5-entry candidate list to find `orient.js` because the install layout differs from the repo layout. Fix: define a proper **shipped-runtime** story — a single versioned directory (e.g. `momentum-runtime/`) installed into targets by `init`/`upgrade`, containing the small dependency-free modules hooks need, with one resolution rule instead of per-file candidate lists. Then the core modules can be the only implementation. Should be settled **before a fourth duplicate is written**. Relates ADR-0016 (eco-event.js constraint), ADR-0017 (E2 detection), BUG-011 (installer ownership predicate). |
| TD-013 | Ecosystem-root discovery is implemented five times with two different algorithms | P2 | open | phase-31b | Surfaced 2026-07-27 in Phase 31b G5 as a live defect, not a code smell. `core/ecosystem/lib/index.js` `findRoot()` walks **UP ONLY**, but `core/ecosystem/layout.md` documents the ecosystem root as a **SIBLING** of its members — which is exactly what `ecosystem init` + `ecosystem add ../<repo>` produce. Every other discovery path already scans siblings: `core/ecosystem/scripts/session-append.sh`, `core/scripts/sessionstart-handoff.sh`, `core/git-hooks/eco-event.js`, `core/ecosystem/lib/cross-repo.js`, and `bin/ecosystem.js`'s own `resolveEcosystemRoot`. `findRoot` is the outlier. **How it stayed hidden:** the hooks always used their own sibling-aware resolver, so nothing exercised `findRoot` from a member repo until `core/ecosystem/lib/events.js` `recordEvent()` did — at which point it silently returned `no ecosystem` for the standard layout, and the Phase 31b `land` event never recorded. Failure mode is silence, which is why it survived 31a's test suite. Worked around in 31b by adding `resolveEcosystemRootFrom()` to events.js (mirroring the documented algorithm) rather than changing `findRoot`, whose up-only semantics other callers may depend on. Fix: pick ONE resolver, document which semantics are correct, and have every caller use it — folding naturally into TD-012's shipped-runtime work, since three of the five copies exist only because hooks cannot import core. Relates TD-012, ENH-022 (MAX_PARENT_WALK), ADR-0016, ADR-0017. |
| ENH-063 | `momentum lanes close` removes only the worktree — never deletes the branch or clears lane state | P2 | open | phase-27 | Discovered 2026-07-09 while reviewing BUG-026. `cmdClose` (`bin/lanes.js:197-212`) flips status to `closed` and, only with `--rm-worktree`, runs `git worktree remove --force` — it **never** `git branch -d`s the lane branch and never clears the lane's manifest/inbox/signals under `<git-common-dir>/momentum/lanes/<id>`. So even the one explicit cleanup path leaves a dangling branch (local + remote) and lingering state. **Fix (Phase 27 G3):** route `lanes close` through the shared `cleanupTarget()` so it does full cleanup (worktree + branch + state), consistent with the synchronous and reconcile cleanup entry points. Relates BUG-026. |
| BUG-007 | Codex hooks `apply_patch\|shell` matcher silently misses every Bash call | P1 | resolved | post-v0.20.1 | Surfaced during 2026-06-13 audit-verification pass. Official Codex docs ([Hooks](https://developers.openai.com/codex/hooks)) state the canonical `tool_name` emitted for shell commands is `"Bash"` (PreToolUse matchers use `Bash`, `apply_patch`, `Edit\|Write`, or MCP names). Momentum's `adapters/codex/hooks.json` uses `apply_patch\|shell` — `shell` never matches in real Codex runtime, so `brainstorm-gate.sh` + `check-history-reminder.sh` are completely bypassed on every `Bash` tool call. The existing synthetic test passes only because `tests/_helpers.js::payloads.codexShell()` injects `toolName: 'shell'` itself. Fix: change matcher to `apply_patch\|Bash` in both PreToolUse and PostToolUse; rename helper to `codexBash`; reframe tests against real `tool_name: "Bash"`. Validate live once VAL-001 lands. |
| BUG-027 | Generated AGENTS.md "shipped recipe set" table row for `sync-config` is missing its trailing `|` (malformed markdown row) | P3 | resolved | phase-29 | Surfaced 2026-07-09 during the Phase 27 self-repo upgrade dogfood: regenerating AGENTS.md produced `\| sync-config \| Re-infer project shape, show config drift, apply on approval (ENH-062)` with NO closing pipe, while every other row is well-formed. The recipe table is generated (row text derived from each recipe's description), so the defect ships to every fresh codex/antigravity/opencode install's AGENTS.md since ENH-062 (Phase 26). Cosmetic (GitHub still renders the table) but sloppy. Fix: find the AGENTS.md recipe-table generator (row assembly for sync-config) and ensure the trailing `\|` is always emitted; re-baseline the 3 non-claude adapter fingerprints. Not fixed in Phase 27 (out of the cleanup theme + would drift fingerprints). **Resolved — verified already fixed in Phase 28** (2026-07-10, Phase 29 G3a): every generated instruction file's `sync-config` row ends with `|`. The premise was mistaken — the recipe table is STATIC prose in each adapter's `surfaces.md`, never generated from descriptions; Phase 28 regenerated the templates correctly but left this row `open`. Phase 29 adds a well-formed-row regression guard in `tests/instruction-generation.test.js`. |
| BUG-028 | Ecosystem session log is dead code on Claude Code — PostToolUse matcher `Edit\|Write` can never deliver the `Bash` events the hook branches on | P1 | resolved | phase-31a | **Resolved 2026-07-27 (Phase 31a G0):** matcher → `Edit\|Write\|Bash` in the shipped adapter and this repo's own `.claude/settings.json`. The durable half is `tests/hook-matcher-reachability.test.js`, which reads the INSTALLED adapter config and fails when a hook script guards on a tool its own matcher can never deliver — closing the BUG-007/BUG-028 CLASS across all four adapters rather than this instance. Proven per Rule 12 by running it against the unfixed code (2 failures with the exact diagnostic, then 4/4 green). G1 additionally supersedes the agent-hook write path entirely with git-native capture, so the session log no longer depends on any matcher. | Surfaced 2026-07-26 grounding five multi-repo session retrospectives against the code. `core/scripts/check-history-reminder.sh:95` guards the ecosystem session-log append on `tool_name = "Bash"` — the ONLY code path that writes commit/PR events into `<eco>/sessions/`. But `adapters/claude-code/settings.json:15` registers that hook with matcher `"Edit\|Write"`, so Bash tool calls never reach it and the branch is unreachable. Verified live against this repo's own installed `.claude/settings.json`. Codex uses `apply_patch\|Bash`; opencode's plugin delegates bash calls explicitly (`.opencode/plugins/momentum.js:135`) — **Claude Code, the default adapter, is the only one broken.** `tests/ecosystem-hook.test.js:140` passes because it pipes `{tool_name:'Bash'}` straight into the script, bypassing the matcher entirely. This is **BUG-007's exact class** (Codex `apply_patch\|shell` never matching the canonical `Bash`) recurring on a second adapter, with the identical synthetic-test-masks-dead-wiring signature. Explains the `sessions/` = only `.gitkeep` evidence reported independently by two of the five sessions despite ~10 commits and 5 PRs. Secondary casualty: the same matcher gap means `brainstorm-gate.sh`'s shell-redirect detection (`> specs/…`) can never fire on Claude Code either. Fix (Phase 31a G0): matcher → `Edit\|Write\|Bash`, **plus** a regression test that reads the INSTALLED adapter settings and fails when a hook guards on a tool its own matcher cannot deliver — closing the class across all four adapters, not the instance. |
