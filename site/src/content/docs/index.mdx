---
title: momentum
description: Spec-driven development for agentic AI. Phases, decisions, history, backlog — first-class state across any AI IDE.
template: splash
hero:
  tagline: Spec-driven development for agentic AI.
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/avinash-singh-io/momentum
      icon: external
      variant: minimal
---

import PhaseFlow from '../../components/diagrams/PhaseFlow.astro';
import Topology from '../../components/diagrams/Topology.astro';
import IDEMatrix from '../../components/IDEMatrix.astro';
import FeatureGrid from '../../components/FeatureGrid.astro';
import InstallSnippet from '../../components/InstallSnippet.astro';
import Personas from '../../components/Personas.astro';
import OrchestrationOverview from '../../components/OrchestrationOverview.astro';
import DecisionTree from '../../components/DecisionTree.astro';
import RulesCallout from '../../components/RulesCallout.astro';
import SkillsPreview from '../../components/SkillsPreview.astro';

<div class="lead-block">

Your AI coding agent forgets. Hit the context limit or start a fresh session
and it loses the roadmap, the architecture decisions, the bug it fixed an hour
ago. **momentum makes your project's memory durable** — phases, decisions,
history, and backlog live as plain files your agent reads at the start of every
session and updates as it works. Whether your agent ships code, manages
infrastructure, runs research, or operates pipelines, the same discipline
applies: every phase planned, every decision recorded, every release tagged.
State that outlives any single session.

<InstallSnippet />

</div>

<section class="how-it-works section-anchor" id="how-it-works" aria-labelledby="how-heading">

## How it works — no daemon, no lock-in {#how-heading}

<p class="section-lead">
momentum isn't a framework or a background service. It's plain files and
conventions your agent already knows how to read.
</p>

1. **`init` writes files into your repo** — your agent's instruction file
   (`CLAUDE.md` / `AGENTS.md`), a `specs/` folder, rule and slash-command
   definitions, and git hooks.
2. **Your AI IDE already reads that instruction file every session** — momentum
   just fills it with a structured workflow and a map of where project state lives.
3. **Your agent maintains the state as it works** — it reads `status.md` to
   orient, logs decisions to `history.md`, and tracks work in `backlog.md`.
   Automatically.

Nothing runs in the background. **Uninstall = delete the files.**

</section>

<PhaseFlow />

<IDEMatrix />

<FeatureGrid />

<RulesCallout />

<SkillsPreview />

<Personas />

<section class="scales section-anchor" id="scale-up" aria-labelledby="scales-heading">

## When one repo isn't enough {#scales-heading}

<p class="section-lead">
Single-project usage is the default — everything above works in one repo with
zero extra setup. When you have related repos that need to coordinate, ecosystem
mode lets <strong>one agent session</strong> work across all of them — no context
switching, full ecosystem awareness, every member's per-repo discipline preserved.
</p>

<Topology />

</section>

<OrchestrationOverview />

<DecisionTree />
